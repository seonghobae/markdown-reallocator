{"config":{"lang":["en"],"separator":"[\\s\\-,:!=\\[\\]()\"/]+|(?!\\b)(?=[A-Z][a-z])|\\.(?!\\d)|&[lg]t;","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Markdown Reallocator","text":"<p>Local-first markdown processing toolkit with semantic operations using embeddings.</p> <ul> <li> <p> Fast &amp; Local</p> <p>Runs entirely on your machine with no API costs. Designed for GPUs with 2-4GB VRAM.</p> <p> Get Started</p> </li> <li> <p> Modular Design</p> <p>Use only the features you need. Python API and CLI for flexible integration.</p> <p> User Guide</p> </li> <li> <p> Semantic Operations</p> <p>Search, reorder, and deduplicate using embeddings. Powered by Ollama and embeddinggemma.</p> <p> Examples</p> </li> <li> <p> Developer Friendly</p> <p>Clean Python API, comprehensive docs, 93%+ test coverage. MIT licensed.</p> <p> API Reference</p> </li> </ul>"},{"location":"#overview","title":"Overview","text":"<p>Markdown Reallocator solves common problems when working with LLM-generated markdown:</p>"},{"location":"#structure-normalization","title":"Structure Normalization","text":"<p>Automatically converts bold titles (<code>**Title**</code>) to proper headings (<code>## Title</code>). LLMs frequently use bold formatting instead of proper heading syntax, breaking document structure and navigation.</p>"},{"location":"#semantic-chunking","title":"Semantic Chunking","text":"<p>Splits documents by headers while preserving metadata and hierarchy. Each chunk maintains context about its position in the document structure.</p>"},{"location":"#intelligent-reordering","title":"Intelligent Reordering","text":"<p>Organizes chunks by semantic similarity to improve topical flow. Choose between sequential (chain similar content) or clustering (group by topics).</p>"},{"location":"#semantic-search","title":"Semantic Search","text":"<p>Find related content using natural language queries. Powered by embeddings for accurate semantic matching beyond keyword search.</p>"},{"location":"#smart-deduplication","title":"Smart Deduplication","text":"<p>Remove redundant sections intelligently. Optional LLM-based merging preserves unique information from all duplicates.</p>"},{"location":"#quick-example","title":"Quick Example","text":"CLIPython API <pre><code># Normalize markdown structure\nmarkdown-reallocator preprocess input.md --output clean.md\n\n# Split into chunks and generate embeddings\nmarkdown-reallocator split clean.md --output chunks.json\nmarkdown-reallocator embed chunks.json --output embeddings.npz\n\n# Search for related content\nmarkdown-reallocator search \"authentication methods\" \\\\\n  chunks.json embeddings.npz --top-k 5\n\n# Reorder by semantic similarity\nmarkdown-reallocator reorder chunks.json embeddings.npz \\\\\n  --strategy cluster --output reordered.md\n</code></pre> <pre><code>from markdown_reallocator.core.preprocessor import MarkdownPreprocessor\nfrom markdown_reallocator.core.splitter import MarkdownSplitter\nfrom markdown_reallocator.core.embedder import Embedder\nfrom markdown_reallocator.modules.search import SearchModule\n\n# Load and preprocess\nmarkdown = Path(\"input.md\").read_text()\npreprocessor = MarkdownPreprocessor()\nclean = preprocessor.preprocess(markdown)\n\n# Split and embed\nsplitter = MarkdownSplitter(max_tokens_per_chunk=500)\nchunks = splitter.split(clean)\n\nembedder = Embedder()\nembedded_chunks = embedder.embed_batch(chunks)\n\n# Search\nsearch = SearchModule(embedder=embedder, top_k=5)\nresults = search.search(\"authentication methods\", embedded_chunks)\n\n# Display results\nfor chunk, score in results:\n    print(f\"Score: {score:.3f}\")\n    print(chunk.content[:100])\n    print()\n</code></pre>"},{"location":"#features","title":"Features","text":"<p> Local-First - No cloud APIs required  Resource-Efficient - Runs on GTX 1050 (2GB VRAM)  Modular - Use only what you need  Fast - Embedding at 300+ tokens/sec  Well-Tested - 93.86% code coverage  Type-Safe - Full type hints with mypy validation</p>"},{"location":"#hardware-requirements","title":"Hardware Requirements","text":""},{"location":"#minimum","title":"Minimum","text":"<ul> <li>GPU: NVIDIA GPU with 2GB VRAM (GTX 1050 or better)</li> <li>CPU: x86_64 processor</li> <li>RAM: 4GB system memory</li> <li>Storage: 2GB for models and cache</li> </ul>"},{"location":"#cpu-only-mode","title":"CPU-Only Mode","text":"<p>Works without GPU but significantly slower: - Embedding: ~10x slower - Search/Reorder: Similar speed (CPU-bound operations)</p> <p>Older CPUs</p> <p>For pre-2011 CPUs without AVX2 support: <pre><code>export NPY_DISABLE_CPU_FEATURES=\"AVX2,FMA3\"\nexport OPENBLAS_CORETYPE=Haswell\n</code></pre></p>"},{"location":"#next-steps","title":"Next Steps","text":"<ul> <li> <p> Installation</p> <p>Install Ollama, download models, and set up Markdown Reallocator</p> </li> <li> <p> Quick Start</p> <p>Get started in 5 minutes with your first workflow</p> </li> <li> <p> User Guide</p> <p>Learn about all features in depth</p> </li> <li> <p> API Reference</p> <p>Complete API documentation with examples</p> </li> </ul>"},{"location":"#performance","title":"Performance","text":"<p>Typical performance on GTX 1050 (2GB) + Intel Xeon X5650:</p> Operation Time Notes Preprocessing &lt;1s per 1000 lines CPU-bound, regex-based Embedding (50 chunks) ~10s GPU-bound Search (1000 chunks) &lt;0.5s NumPy vectorized Reordering (100 chunks) &lt;2s Similarity + clustering"},{"location":"#community","title":"Community","text":"<ul> <li> GitHub Repository</li> <li> Issue Tracker</li> <li> Discussions</li> </ul>"},{"location":"#license","title":"License","text":"<p>Markdown Reallocator is released under the MIT License.</p>"},{"location":"troubleshooting/","title":"Troubleshooting","text":"<p>Common issues and solutions.</p>"},{"location":"troubleshooting/#installation-issues","title":"Installation Issues","text":""},{"location":"troubleshooting/#ollama-not-found","title":"Ollama Not Found","text":"<p>Problem: <code>Error: could not connect to ollama</code></p> <p>Solution: <pre><code># Start Ollama\nollama serve\n\n# Verify\nollama list\n</code></pre></p>"},{"location":"troubleshooting/#gpu-out-of-memory","title":"GPU Out of Memory","text":"<p>Problem: <code>CUDA out of memory</code></p> <p>Solutions: 1. Reduce batch size:    <pre><code>export MARKDOWN_REALLOCATOR_EMBEDDER_BATCH_SIZE=5\n</code></pre></p> <ol> <li>Use CPU-only mode:    <pre><code>export CUDA_VISIBLE_DEVICES=\"\"\n</code></pre></li> </ol>"},{"location":"troubleshooting/#avx2-errors","title":"AVX2 Errors","text":"<p>Problem: <code>Illegal instruction (core dumped)</code></p> <p>Solution: <pre><code># Add to ~/.bashrc\nexport NPY_DISABLE_CPU_FEATURES=\"AVX2,FMA3\"\nexport OPENBLAS_CORETYPE=Haswell\n</code></pre></p>"},{"location":"troubleshooting/#runtime-issues","title":"Runtime Issues","text":""},{"location":"troubleshooting/#slow-embedding","title":"Slow Embedding","text":"<p>Causes: - Using CPU instead of GPU - Large batch size - Model not cached</p> <p>Solutions: <pre><code># Check GPU usage\nnvidia-smi\n\n# Enable caching\nmarkdown-reallocator embed chunks.json --output embeddings.npz --cache\n</code></pre></p>"},{"location":"troubleshooting/#import-errors","title":"Import Errors","text":"<p>Problem: <code>ModuleNotFoundError</code></p> <p>Solution: <pre><code># Verify installation\npip list | grep markdown-reallocator\n\n# Reinstall\npip uninstall markdown-reallocator\npip install markdown-reallocator\n</code></pre></p>"},{"location":"troubleshooting/#getting-help","title":"Getting Help","text":"<ul> <li>GitHub Issues</li> <li>Discussions</li> <li>Documentation</li> </ul>"},{"location":"api/","title":"API Reference","text":"<p>Complete API documentation auto-generated from source code.</p>"},{"location":"api/#core-modules","title":"Core Modules","text":"<p>Process markdown documents:</p> <ul> <li>Preprocessor - Fix markdown structure</li> <li>Splitter - Create semantic chunks</li> <li>Embedder - Generate embeddings</li> </ul>"},{"location":"api/#application-modules","title":"Application Modules","text":"<p>Semantic operations:</p> <ul> <li>Search - Find similar content</li> <li>Reorder - Organize by similarity</li> <li>Deduplication - Remove duplicates</li> </ul>"},{"location":"api/#data-models","title":"Data Models","text":"<p>Core data structures:</p> <ul> <li>Chunk - Document chunk with metadata</li> <li>Embedding - Vector representation</li> </ul>"},{"location":"api/#utilities","title":"Utilities","text":"<p>Helper functions:</p> <ul> <li>Cache - Embedding caching</li> <li>Memory - Memory management</li> <li>Similarity - Similarity calculations</li> </ul>"},{"location":"api/#quick-links","title":"Quick Links","text":"<ul> <li>User Guide - Learn how to use the API</li> <li>Examples - See code examples</li> <li>Configuration - Configure behavior</li> </ul>"},{"location":"api/core/embedder/","title":"Embedder","text":""},{"location":"api/core/embedder/#markdown_reallocator.core.embedder","title":"markdown_reallocator.core.embedder","text":"<p>Embedder for generating semantic embeddings using Ollama.</p> <p>This module provides embedding generation for markdown chunks using embeddinggemma with caching and memory management.</p>"},{"location":"api/core/embedder/#markdown_reallocator.core.embedder.Embedder","title":"Embedder","text":"<pre><code>Embedder(model_name: str = 'embeddinggemma', cache_enabled: bool = True, cache_max_size: int = 1000, batch_size: int = 10, memory_threshold_mb: float = 1500.0)\n</code></pre> <p>Embedder for generating semantic embeddings using Ollama.</p> <p>This class provides embedding generation with caching, memory monitoring, and batch processing capabilities.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; embedder = Embedder()\n&gt;&gt;&gt; chunk = Chunk(chunk_id=\"test\", content=\"Hello world\", metadata=ChunkMetadata())\n&gt;&gt;&gt; embedded_chunk = embedder.embed_chunk(chunk)\n&gt;&gt;&gt; embedded_chunk.embedding.shape\n(384,)\n</code></pre> <p>Initialize embedder with configuration.</p> <p>Parameters:</p> Name Type Description Default <code>model_name</code> <code>str</code> <p>Name of Ollama embedding model (default: \"embeddinggemma\")</p> <code>'embeddinggemma'</code> <code>cache_enabled</code> <code>bool</code> <p>Whether to enable embedding cache</p> <code>True</code> <code>cache_max_size</code> <code>int</code> <p>Maximum cache size (number of embeddings)</p> <code>1000</code> <code>batch_size</code> <code>int</code> <p>Number of chunks to process in each batch</p> <code>10</code> <code>memory_threshold_mb</code> <code>float</code> <p>Maximum GPU memory usage in MB</p> <code>1500.0</code> Source code in <code>markdown_reallocator/core/embedder.py</code> <pre><code>def __init__(\n    self,\n    model_name: str = \"embeddinggemma\",\n    cache_enabled: bool = True,\n    cache_max_size: int = 1000,\n    batch_size: int = 10,\n    memory_threshold_mb: float = 1500.0,\n):\n    \"\"\"Initialize embedder with configuration.\n\n    Args:\n        model_name: Name of Ollama embedding model (default: \"embeddinggemma\")\n        cache_enabled: Whether to enable embedding cache\n        cache_max_size: Maximum cache size (number of embeddings)\n        batch_size: Number of chunks to process in each batch\n        memory_threshold_mb: Maximum GPU memory usage in MB\n    \"\"\"\n    self.model_name = model_name\n    self.cache_enabled = cache_enabled\n    self.batch_size = batch_size\n    self.memory_threshold_mb = memory_threshold_mb\n\n    # Initialize cache if enabled\n    self._cache: EmbeddingCache | None = None\n    if cache_enabled:\n        self._cache = EmbeddingCache(max_size=cache_max_size)\n\n    # Lazy-loaded Ollama client\n    self._client: ollama.Client | None = None\n    self._model_loaded = False\n</code></pre>"},{"location":"api/core/embedder/#markdown_reallocator.core.embedder.Embedder.embed_chunk","title":"embed_chunk","text":"<pre><code>embed_chunk(chunk: Chunk, force_refresh: bool = False) -&gt; Chunk\n</code></pre> <p>Generate embedding for a single chunk.</p> <p>Uses cache if enabled and embedding is cached. Updates chunk.embedding in-place and returns the modified chunk.</p> <p>Parameters:</p> Name Type Description Default <code>chunk</code> <code>Chunk</code> <p>Chunk to embed</p> required <code>force_refresh</code> <code>bool</code> <p>Force regeneration even if cached</p> <code>False</code> <p>Returns:</p> Type Description <code>Chunk</code> <p>Chunk with embedding populated</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If model loading or embedding generation fails</p> Source code in <code>markdown_reallocator/core/embedder.py</code> <pre><code>def embed_chunk(self, chunk: Chunk, force_refresh: bool = False) -&gt; Chunk:\n    \"\"\"Generate embedding for a single chunk.\n\n    Uses cache if enabled and embedding is cached. Updates chunk.embedding\n    in-place and returns the modified chunk.\n\n    Args:\n        chunk: Chunk to embed\n        force_refresh: Force regeneration even if cached\n\n    Returns:\n        Chunk with embedding populated\n\n    Raises:\n        RuntimeError: If model loading or embedding generation fails\n    \"\"\"\n    # Compute text hash for cache lookup\n    text_hash = self._compute_text_hash(chunk.content)\n\n    # Check cache if enabled\n    if self.cache_enabled and not force_refresh and self._cache is not None:\n        cached_embedding = self._cache.get(text_hash, self.model_name)\n        if cached_embedding is not None:\n            chunk.embedding = cached_embedding\n            logger.debug(f\"Cache hit for chunk {chunk.chunk_id}\")\n            return chunk\n\n    # Ensure model is loaded\n    self._ensure_model_loaded()\n\n    if not self._client:\n        raise RuntimeError(\"Ollama client not initialized\")\n\n    try:\n        # Generate embedding with GPU monitoring\n        with GPUMemoryMonitor(warn_threshold=0.9):\n            response = self._client.embeddings(\n                model=self.model_name,\n                prompt=chunk.content\n            )\n\n        # Extract embedding\n        if \"embedding\" not in response:\n            raise RuntimeError(\n                f\"Model '{self.model_name}' did not return embedding\"\n            )\n\n        embedding = np.array(response[\"embedding\"], dtype=np.float32)\n\n        # Update chunk\n        chunk.embedding = embedding\n\n        # Cache if enabled\n        if self.cache_enabled and self._cache is not None:\n            self._cache.put(text_hash, self.model_name, embedding)\n            logger.debug(\n                f\"Cached embedding for chunk {chunk.chunk_id} \"\n                f\"(text_hash={text_hash}, model={self.model_name})\"\n            )\n\n        logger.debug(\n            f\"Generated embedding for chunk {chunk.chunk_id}: \"\n            f\"dim={len(embedding)}\"\n        )\n\n        return chunk\n\n    except Exception as e:\n        logger.error(f\"Failed to generate embedding for chunk {chunk.chunk_id}: {e}\")\n        raise RuntimeError(f\"Embedding generation failed: {e}\") from e\n</code></pre>"},{"location":"api/core/embedder/#markdown_reallocator.core.embedder.Embedder.embed_batch","title":"embed_batch","text":"<pre><code>embed_batch(chunks: list[Chunk], progress_callback: Callable[[int, int], None] | None = None) -&gt; list[Chunk]\n</code></pre> <p>Generate embeddings for multiple chunks in batches.</p> <p>Processes chunks in batches to manage memory and provide progress updates. Individual failures are logged but don't halt the entire process.</p> <p>Parameters:</p> Name Type Description Default <code>chunks</code> <code>list[Chunk]</code> <p>List of chunks to embed</p> required <code>progress_callback</code> <code>Callable[[int, int], None] | None</code> <p>Optional callback(current, total) for progress tracking</p> <code>None</code> <p>Returns:</p> Type Description <code>list[Chunk]</code> <p>List of chunks with embeddings populated</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If model loading fails</p> Source code in <code>markdown_reallocator/core/embedder.py</code> <pre><code>def embed_batch(\n    self,\n    chunks: list[Chunk],\n    progress_callback: Callable[[int, int], None] | None = None,\n) -&gt; list[Chunk]:\n    \"\"\"Generate embeddings for multiple chunks in batches.\n\n    Processes chunks in batches to manage memory and provide progress updates.\n    Individual failures are logged but don't halt the entire process.\n\n    Args:\n        chunks: List of chunks to embed\n        progress_callback: Optional callback(current, total) for progress tracking\n\n    Returns:\n        List of chunks with embeddings populated\n\n    Raises:\n        RuntimeError: If model loading fails\n    \"\"\"\n    if not chunks:\n        return []\n\n    # Ensure model is loaded once\n    self._ensure_model_loaded()\n\n    total = len(chunks)\n    embedded_chunks: list[Chunk] = []\n    failed_chunks: list[tuple[str, Exception]] = []\n\n    # Process in batches\n    for batch_start in range(0, total, self.batch_size):\n        batch_end = min(batch_start + self.batch_size, total)\n        batch = chunks[batch_start:batch_end]\n\n        # Process each chunk in batch\n        for chunk in batch:\n            try:\n                self.embed_chunk(chunk)\n                embedded_chunks.append(chunk)\n\n            except Exception as e:\n                logger.warning(\n                    f\"Failed to embed chunk {chunk.chunk_id}: {e}\"\n                )\n                failed_chunks.append((chunk.chunk_id, e))\n                # Still add to list but without embedding\n                embedded_chunks.append(chunk)\n\n        # Report progress\n        if progress_callback:\n            progress_callback(batch_end, total)\n\n    # Log summary\n    if failed_chunks:\n        logger.warning(\n            f\"Batch embedding completed with {len(failed_chunks)} failures \"\n            f\"out of {total} chunks\"\n        )\n        for chunk_id, error in failed_chunks[:5]:  # Log first 5\n            logger.warning(f\"  - {chunk_id}: {error}\")\n\n    else:\n        logger.info(f\"Successfully embedded {total} chunks\")\n\n    return embedded_chunks\n</code></pre>"},{"location":"api/core/embedder/#markdown_reallocator.core.embedder.Embedder.get_cache_stats","title":"get_cache_stats","text":"<pre><code>get_cache_stats() -&gt; dict[str, Any] | None\n</code></pre> <p>Get cache statistics.</p> <p>Returns:</p> Type Description <code>dict[str, Any] | None</code> <p>Dictionary with cache statistics or None if cache disabled</p> Source code in <code>markdown_reallocator/core/embedder.py</code> <pre><code>def get_cache_stats(self) -&gt; dict[str, Any] | None:\n    \"\"\"Get cache statistics.\n\n    Returns:\n        Dictionary with cache statistics or None if cache disabled\n    \"\"\"\n    if not self.cache_enabled or self._cache is None:\n        return None\n\n    stats = self._cache.stats\n    return {\n        \"hits\": stats.hits,\n        \"misses\": stats.misses,\n        \"hit_rate\": stats.hit_rate,\n        \"current_size\": stats.current_size,\n        \"max_size\": stats.max_size,\n    }\n</code></pre>"},{"location":"api/core/embedder/#markdown_reallocator.core.embedder.Embedder.clear_cache","title":"clear_cache","text":"<pre><code>clear_cache() -&gt; None\n</code></pre> <p>Clear the embedding cache.</p> Source code in <code>markdown_reallocator/core/embedder.py</code> <pre><code>def clear_cache(self) -&gt; None:\n    \"\"\"Clear the embedding cache.\"\"\"\n    if self._cache is not None:\n        self._cache.clear()\n        logger.info(\"Embedding cache cleared\")\n</code></pre>"},{"location":"api/core/embedder/#markdown_reallocator.core.embedder.Embedder.save_embeddings","title":"save_embeddings","text":"<pre><code>save_embeddings(chunks: list[Chunk], filepath: str) -&gt; None\n</code></pre> <p>Save chunk embeddings to .npz file.</p> <p>Parameters:</p> Name Type Description Default <code>chunks</code> <code>list[Chunk]</code> <p>List of chunks with embeddings</p> required <code>filepath</code> <code>str</code> <p>Path to save .npz file</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If chunks have no embeddings</p> Source code in <code>markdown_reallocator/core/embedder.py</code> <pre><code>def save_embeddings(\n    self,\n    chunks: list[Chunk],\n    filepath: str,\n) -&gt; None:\n    \"\"\"Save chunk embeddings to .npz file.\n\n    Args:\n        chunks: List of chunks with embeddings\n        filepath: Path to save .npz file\n\n    Raises:\n        ValueError: If chunks have no embeddings\n    \"\"\"\n    # Filter chunks with embeddings\n    embedded_chunks = [c for c in chunks if c.embedding is not None]\n\n    if not embedded_chunks:\n        raise ValueError(\"No chunks have embeddings to save\")\n\n    # Prepare data - dict values must be non-None\n    embeddings_dict: dict[str, np.ndarray] = {}\n    for c in embedded_chunks:\n        if c.embedding is not None:  # Type guard for mypy\n            embeddings_dict[c.chunk_id] = c.embedding\n\n    # Save as npz\n    np.savez(filepath, **embeddings_dict)\n    logger.info(\n        f\"Saved {len(embedded_chunks)} embeddings to {filepath}\"\n    )\n</code></pre>"},{"location":"api/core/embedder/#markdown_reallocator.core.embedder.Embedder.load_embeddings","title":"load_embeddings","text":"<pre><code>load_embeddings(chunks: list[Chunk], filepath: str) -&gt; list[Chunk]\n</code></pre> <p>Load chunk embeddings from .npz file.</p> <p>Parameters:</p> Name Type Description Default <code>chunks</code> <code>list[Chunk]</code> <p>List of chunks to populate with embeddings</p> required <code>filepath</code> <code>str</code> <p>Path to .npz file</p> required <p>Returns:</p> Type Description <code>list[Chunk]</code> <p>List of chunks with embeddings populated</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If filepath doesn't exist</p> Source code in <code>markdown_reallocator/core/embedder.py</code> <pre><code>def load_embeddings(\n    self,\n    chunks: list[Chunk],\n    filepath: str,\n) -&gt; list[Chunk]:\n    \"\"\"Load chunk embeddings from .npz file.\n\n    Args:\n        chunks: List of chunks to populate with embeddings\n        filepath: Path to .npz file\n\n    Returns:\n        List of chunks with embeddings populated\n\n    Raises:\n        FileNotFoundError: If filepath doesn't exist\n    \"\"\"\n    # Load npz file\n    embeddings_data = np.load(filepath)\n\n    # Match embeddings to chunks by ID\n    loaded_count = 0\n    for chunk in chunks:\n        if chunk.chunk_id in embeddings_data:\n            chunk.embedding = embeddings_data[chunk.chunk_id]\n            loaded_count += 1\n\n    logger.info(\n        f\"Loaded {loaded_count}/{len(chunks)} embeddings from {filepath}\"\n    )\n\n    return chunks\n</code></pre>"},{"location":"api/core/preprocessor/","title":"Preprocessor","text":""},{"location":"api/core/preprocessor/#markdown_reallocator.core.preprocessor","title":"markdown_reallocator.core.preprocessor","text":"<p>Markdown preprocessor with bold-to-heading detection.</p> <p>This module provides rule-based markdown normalization that detects LLM-generated bold titles and converts them to proper headings.</p>"},{"location":"api/core/preprocessor/#markdown_reallocator.core.preprocessor.MarkdownPreprocessor","title":"MarkdownPreprocessor","text":"<pre><code>MarkdownPreprocessor(config: PreprocessorConfig | None = None)\n</code></pre> <p>Preprocessor for detecting and converting bold titles to headings.</p> <p>This class implements rule-based heuristics to distinguish between bold text used for titles (which should be headings) and bold text used for emphasis (which should remain as-is).</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; preprocessor = MarkdownPreprocessor()\n&gt;&gt;&gt; markdown = \"**Introduction**\\n\\nSome text here.\"\n&gt;&gt;&gt; result = preprocessor.preprocess(markdown)\n&gt;&gt;&gt; print(result)\n## Introduction\n</code></pre> <p>Some text here.</p> <p>Initialize preprocessor with configuration.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>PreprocessorConfig | None</code> <p>Configuration object. If None, uses default config.</p> <code>None</code> Source code in <code>markdown_reallocator/core/preprocessor.py</code> <pre><code>def __init__(self, config: PreprocessorConfig | None = None):\n    \"\"\"Initialize preprocessor with configuration.\n\n    Args:\n        config: Configuration object. If None, uses default config.\n    \"\"\"\n    self.config = config or PreprocessorConfig()\n    self._in_code_block = False\n    self._in_list = False\n    self._in_table = False\n    self._heading_levels: list[int] = []  # Track heading levels seen\n</code></pre>"},{"location":"api/core/preprocessor/#markdown_reallocator.core.preprocessor.MarkdownPreprocessor.detect_bold_title","title":"detect_bold_title","text":"<pre><code>detect_bold_title(line: str, context: list[str], line_index: int) -&gt; bool\n</code></pre> <p>Detect if bold text on a line is a title.</p> <p>Uses heuristics to distinguish title bold from emphasis bold: - Standalone on line (only whitespace around bold) - Empty lines before/after (or at document boundaries) - Line length within title threshold - Capitalization patterns (title case or first letter uppercase)</p> <p>Parameters:</p> Name Type Description Default <code>line</code> <code>str</code> <p>The line to check</p> required <code>context</code> <code>list[str]</code> <p>List of all lines in document</p> required <code>line_index</code> <code>int</code> <p>Index of current line in context</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if bold text appears to be a title</p> Source code in <code>markdown_reallocator/core/preprocessor.py</code> <pre><code>def detect_bold_title(\n    self,\n    line: str,\n    context: list[str],\n    line_index: int\n) -&gt; bool:\n    \"\"\"Detect if bold text on a line is a title.\n\n    Uses heuristics to distinguish title bold from emphasis bold:\n    - Standalone on line (only whitespace around bold)\n    - Empty lines before/after (or at document boundaries)\n    - Line length within title threshold\n    - Capitalization patterns (title case or first letter uppercase)\n\n    Args:\n        line: The line to check\n        context: List of all lines in document\n        line_index: Index of current line in context\n\n    Returns:\n        True if bold text appears to be a title\n    \"\"\"\n    # Skip if in protected contexts\n    if self.config.preserve_code_blocks and self._in_code_block:\n        return False\n    if self.config.preserve_lists and self._in_list:\n        return False\n    if self.config.preserve_tables and self._in_table:\n        return False\n\n    # Check if line contains bold pattern\n    bold_pattern = re.compile(r'^\\s*\\*\\*(.+?)\\*\\*\\s*$')\n    match = bold_pattern.match(line)\n\n    if not match:\n        return False\n\n    bold_text = match.group(1)\n\n    # Check length constraint\n    if len(bold_text) &gt; self.config.max_title_length:\n        if self.config.log_ambiguous_cases:\n            logger.warning(\n                f\"Bold text exceeds max title length ({len(bold_text)} &gt; \"\n                f\"{self.config.max_title_length}): {bold_text[:50]}...\"\n            )\n        return False\n\n    # Check empty lines before/after if required\n    if self.config.require_empty_lines:\n        has_empty_before = (\n            line_index == 0 or\n            context[line_index - 1].strip() == \"\"\n        )\n        has_empty_after = (\n            line_index == len(context) - 1 or\n            context[line_index + 1].strip() == \"\"\n        )\n\n        if not (has_empty_before and has_empty_after):\n            return False\n\n    # Check capitalization patterns\n    letters = [c for c in bold_text if c.isalpha()]\n    if not letters:\n        return False\n\n    # Check if first letter is uppercase (minimum requirement)\n    first_char = next((c for c in bold_text if c.isalpha()), None)\n    if not first_char or not first_char.isupper():\n        return False\n\n    # Check for title case if enabled (do this before ratio check)\n    if self.config.detect_title_case:\n        words = bold_text.split()\n        if len(words) &gt; 1:\n            # Title case: most words start with uppercase\n            title_case_count = sum(1 for word in words if word and word[0].isalpha() and word[0].isupper())\n            title_case_ratio = title_case_count / len(words)\n\n            # If it looks like title case, it's likely a title\n            if title_case_ratio &gt;= 0.5:\n                return True\n        else:\n            # Single word with first letter uppercase is likely a title\n            # (given it passes other checks like empty lines)\n            return True\n\n    # For non-title-case detection or when title case disabled,\n    # check capitalization ratio\n    uppercase_count = sum(1 for c in letters if c.isupper())\n    capitalization_ratio = uppercase_count / len(letters)\n\n    if capitalization_ratio &lt; self.config.min_capitalization_ratio:\n        return False\n\n    return True\n</code></pre>"},{"location":"api/core/preprocessor/#markdown_reallocator.core.preprocessor.MarkdownPreprocessor.infer_heading_level","title":"infer_heading_level","text":"<pre><code>infer_heading_level(context: list[str], line_index: int) -&gt; int\n</code></pre> <p>Infer appropriate heading level from document context.</p> <p>Determines heading level (H2 or H3) based on surrounding structure: - Start with H2 if no headings seen yet - Use H3 if previous heading was H2 - Avoid heading level jumps</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>list[str]</code> <p>List of all lines in document</p> required <code>line_index</code> <code>int</code> <p>Index of current line in context</p> required <p>Returns:</p> Type Description <code>int</code> <p>Heading level (2 or 3)</p> Source code in <code>markdown_reallocator/core/preprocessor.py</code> <pre><code>def infer_heading_level(\n    self,\n    context: list[str],\n    line_index: int\n) -&gt; int:\n    \"\"\"Infer appropriate heading level from document context.\n\n    Determines heading level (H2 or H3) based on surrounding structure:\n    - Start with H2 if no headings seen yet\n    - Use H3 if previous heading was H2\n    - Avoid heading level jumps\n\n    Args:\n        context: List of all lines in document\n        line_index: Index of current line in context\n\n    Returns:\n        Heading level (2 or 3)\n    \"\"\"\n    # Look backwards for the most recent actual heading in context\n    for i in range(line_index - 1, -1, -1):\n        line = context[i].strip()\n        if line.startswith(\"#\"):\n            # Count heading level\n            level = len(line) - len(line.lstrip(\"#\"))\n            if level &gt; 0:\n                # Next heading should be same level or one level deeper\n                return min(level + 1, 3)\n\n    # If no headings found in context, use tracked headings\n    if not self._heading_levels:\n        # Default to H2 for first heading\n        return 2\n\n    # Get last tracked heading level\n    last_level = self._heading_levels[-1]\n\n    # Default progression: H2 \u2192 H3\n    if last_level == 2:\n        return 3\n\n    return 2\n</code></pre>"},{"location":"api/core/preprocessor/#markdown_reallocator.core.preprocessor.MarkdownPreprocessor.convert_to_heading","title":"convert_to_heading","text":"<pre><code>convert_to_heading(bold_text: str, level: int) -&gt; str\n</code></pre> <p>Convert bold text to markdown heading.</p> <p>Parameters:</p> Name Type Description Default <code>bold_text</code> <code>str</code> <p>The text inside bold markers</p> required <code>level</code> <code>int</code> <p>Heading level (1-6)</p> required <p>Returns:</p> Type Description <code>str</code> <p>Markdown heading string</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; preprocessor = MarkdownPreprocessor()\n&gt;&gt;&gt; preprocessor.convert_to_heading(\"Introduction\", 2)\n'## Introduction'\n</code></pre> Source code in <code>markdown_reallocator/core/preprocessor.py</code> <pre><code>def convert_to_heading(\n    self,\n    bold_text: str,\n    level: int\n) -&gt; str:\n    \"\"\"Convert bold text to markdown heading.\n\n    Args:\n        bold_text: The text inside bold markers\n        level: Heading level (1-6)\n\n    Returns:\n        Markdown heading string\n\n    Examples:\n        &gt;&gt;&gt; preprocessor = MarkdownPreprocessor()\n        &gt;&gt;&gt; preprocessor.convert_to_heading(\"Introduction\", 2)\n        '## Introduction'\n    \"\"\"\n    if not 1 &lt;= level &lt;= 6:\n        raise ValueError(f\"Heading level must be 1-6, got {level}\")\n\n    heading_markers = \"#\" * level\n    return f\"{heading_markers} {bold_text}\"\n</code></pre>"},{"location":"api/core/preprocessor/#markdown_reallocator.core.preprocessor.MarkdownPreprocessor.preprocess","title":"preprocess","text":"<pre><code>preprocess(markdown: str) -&gt; str\n</code></pre> <p>Preprocess markdown document.</p> <p>Main processing pipeline that: 1. Splits markdown into lines 2. Tracks context (code blocks, lists, tables) 3. Detects bold titles and converts to headings 4. Preserves all other markdown formatting</p> <p>Parameters:</p> Name Type Description Default <code>markdown</code> <code>str</code> <p>Input markdown string</p> required <p>Returns:</p> Type Description <code>str</code> <p>Processed markdown string</p> Source code in <code>markdown_reallocator/core/preprocessor.py</code> <pre><code>def preprocess(self, markdown: str) -&gt; str:\n    \"\"\"Preprocess markdown document.\n\n    Main processing pipeline that:\n    1. Splits markdown into lines\n    2. Tracks context (code blocks, lists, tables)\n    3. Detects bold titles and converts to headings\n    4. Preserves all other markdown formatting\n\n    Args:\n        markdown: Input markdown string\n\n    Returns:\n        Processed markdown string\n    \"\"\"\n    lines = markdown.split(\"\\n\")\n    result_lines: list[str] = []\n\n    # Reset state\n    self._in_code_block = False\n    self._in_list = False\n    self._in_table = False\n    self._heading_levels = []\n\n    for i, line in enumerate(lines):\n        # Track code block state\n        if self.config.preserve_code_blocks:\n            if line.strip().startswith(\"```\"):\n                self._in_code_block = not self._in_code_block\n\n        # Track list state\n        if self.config.preserve_lists:\n            list_pattern = re.compile(r'^\\s*[-*+]\\s+|\\^\\s*\\d+\\.\\s+')\n            self._in_list = bool(list_pattern.match(line))\n\n        # Track table state\n        if self.config.preserve_tables:\n            self._in_table = \"|\" in line\n\n        # Try to detect and convert bold title\n        if self.detect_bold_title(line, lines, i):\n            # Extract bold text\n            match = re.match(r'^\\s*\\*\\*(.+?)\\*\\*\\s*$', line)\n            if match:\n                bold_text = match.group(1)\n                level = self.infer_heading_level(lines, i)\n                converted = self.convert_to_heading(bold_text, level)\n\n                if self.config.log_ambiguous_cases:\n                    logger.debug(\n                        f\"Converted bold to H{level}: {bold_text}\"\n                    )\n\n                result_lines.append(converted)\n                self._heading_levels.append(level)\n                continue\n\n        # Track actual headings\n        if line.strip().startswith(\"#\"):\n            level = len(line) - len(line.lstrip(\"#\"))\n            if level &gt; 0:\n                self._heading_levels.append(level)\n\n        # Keep line as-is\n        result_lines.append(line)\n\n    return \"\\n\".join(result_lines)\n</code></pre>"},{"location":"api/core/preprocessor/#markdown_reallocator.core.preprocessor.PreprocessorConfig","title":"PreprocessorConfig  <code>dataclass</code>","text":"<pre><code>PreprocessorConfig(max_title_length: int = 80, require_empty_lines: bool = True, min_capitalization_ratio: float = 0.1, detect_title_case: bool = True, preserve_code_blocks: bool = True, preserve_lists: bool = True, preserve_tables: bool = True, log_ambiguous_cases: bool = True)\n</code></pre> <p>Configuration for markdown preprocessing.</p> <p>Attributes:</p> Name Type Description <code>max_title_length</code> <code>int</code> <p>Maximum character length for bold text to be considered a title (default: 80)</p> <code>require_empty_lines</code> <code>bool</code> <p>Whether to require empty lines before/after bold titles (default: True)</p> <code>min_capitalization_ratio</code> <code>float</code> <p>Minimum ratio of uppercase letters to total letters for title detection (default: 0.1)</p> <code>detect_title_case</code> <code>bool</code> <p>Whether to detect title case patterns (default: True)</p> <code>preserve_code_blocks</code> <code>bool</code> <p>Whether to skip processing inside code blocks (default: True)</p> <code>preserve_lists</code> <code>bool</code> <p>Whether to skip processing inside lists (default: True)</p> <code>preserve_tables</code> <code>bool</code> <p>Whether to skip processing inside tables (default: True)</p> <code>log_ambiguous_cases</code> <code>bool</code> <p>Whether to log warnings for ambiguous detections (default: True)</p>"},{"location":"api/core/preprocessor/#markdown_reallocator.core.preprocessor.PreprocessorConfig.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Convert config to dictionary.</p> Source code in <code>markdown_reallocator/core/preprocessor.py</code> <pre><code>def to_dict(self) -&gt; dict[str, Any]:\n    \"\"\"Convert config to dictionary.\"\"\"\n    return {\n        \"max_title_length\": self.max_title_length,\n        \"require_empty_lines\": self.require_empty_lines,\n        \"min_capitalization_ratio\": self.min_capitalization_ratio,\n        \"detect_title_case\": self.detect_title_case,\n        \"preserve_code_blocks\": self.preserve_code_blocks,\n        \"preserve_lists\": self.preserve_lists,\n        \"preserve_tables\": self.preserve_tables,\n        \"log_ambiguous_cases\": self.log_ambiguous_cases,\n    }\n</code></pre>"},{"location":"api/core/preprocessor/#markdown_reallocator.core.preprocessor.PreprocessorConfig.from_dict","title":"from_dict  <code>classmethod</code>","text":"<pre><code>from_dict(config_dict: dict[str, Any]) -&gt; PreprocessorConfig\n</code></pre> <p>Create config from dictionary.</p> Source code in <code>markdown_reallocator/core/preprocessor.py</code> <pre><code>@classmethod\ndef from_dict(cls, config_dict: dict[str, Any]) -&gt; \"PreprocessorConfig\":\n    \"\"\"Create config from dictionary.\"\"\"\n    return cls(**config_dict)\n</code></pre>"},{"location":"api/core/splitter/","title":"Splitter","text":""},{"location":"api/core/splitter/#markdown_reallocator.core.splitter","title":"markdown_reallocator.core.splitter","text":"<p>Markdown splitter using LangChain.</p> <p>This module provides semantic markdown chunking that splits by headers while preserving hierarchy metadata.</p>"},{"location":"api/core/splitter/#markdown_reallocator.core.splitter.MarkdownSplitter","title":"MarkdownSplitter","text":"<pre><code>MarkdownSplitter(headers_to_split_on: list[tuple[str, str]] | None = None, max_tokens_per_chunk: int = 1000)\n</code></pre> <p>Splitter for markdown documents based on header hierarchy.</p> <p>This class wraps LangChain's MarkdownHeaderTextSplitter and converts results into our Chunk/ChunkMetadata format.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; splitter = MarkdownSplitter()\n&gt;&gt;&gt; markdown = \"# Title\\n\\nContent\\n\\n## Section\\n\\nMore content\"\n&gt;&gt;&gt; chunks = splitter.split(markdown)\n&gt;&gt;&gt; len(chunks)\n2\n&gt;&gt;&gt; chunks[0].metadata.h1\n'Title'\n</code></pre> <p>Initialize splitter with configuration.</p> <p>Parameters:</p> Name Type Description Default <code>headers_to_split_on</code> <code>list[tuple[str, str]] | None</code> <p>List of (header_marker, header_name) tuples. Defaults to [(\"#\", \"h1\"), (\"##\", \"h2\"), (\"###\", \"h3\")].</p> <code>None</code> <code>max_tokens_per_chunk</code> <code>int</code> <p>Maximum estimated tokens per chunk. Chunks exceeding this will trigger a warning.</p> <code>1000</code> Source code in <code>markdown_reallocator/core/splitter.py</code> <pre><code>def __init__(\n    self,\n    headers_to_split_on: list[tuple[str, str]] | None = None,\n    max_tokens_per_chunk: int = 1000,\n):\n    \"\"\"Initialize splitter with configuration.\n\n    Args:\n        headers_to_split_on: List of (header_marker, header_name) tuples.\n            Defaults to [(\"#\", \"h1\"), (\"##\", \"h2\"), (\"###\", \"h3\")].\n        max_tokens_per_chunk: Maximum estimated tokens per chunk.\n            Chunks exceeding this will trigger a warning.\n    \"\"\"\n    self.headers_to_split_on = headers_to_split_on or [\n        (\"#\", \"h1\"),\n        (\"##\", \"h2\"),\n        (\"###\", \"h3\"),\n    ]\n    self.max_tokens_per_chunk = max_tokens_per_chunk\n\n    # Initialize LangChain splitter\n    self._splitter = MarkdownHeaderTextSplitter(\n        headers_to_split_on=self.headers_to_split_on,\n        strip_headers=False,  # Keep headers in content\n    )\n</code></pre>"},{"location":"api/core/splitter/#markdown_reallocator.core.splitter.MarkdownSplitter.estimate_tokens","title":"estimate_tokens","text":"<pre><code>estimate_tokens(text: str) -&gt; int\n</code></pre> <p>Estimate token count using simple whitespace-based heuristic.</p> <p>This is a fast approximation. For more accuracy, use tiktoken.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Text to estimate tokens for</p> required <p>Returns:</p> Type Description <code>int</code> <p>Estimated token count (roughly words * 1.3)</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; splitter = MarkdownSplitter()\n&gt;&gt;&gt; splitter.estimate_tokens(\"Hello world this is a test\")\n8\n</code></pre> Source code in <code>markdown_reallocator/core/splitter.py</code> <pre><code>def estimate_tokens(self, text: str) -&gt; int:\n    \"\"\"Estimate token count using simple whitespace-based heuristic.\n\n    This is a fast approximation. For more accuracy, use tiktoken.\n\n    Args:\n        text: Text to estimate tokens for\n\n    Returns:\n        Estimated token count (roughly words * 1.3)\n\n    Examples:\n        &gt;&gt;&gt; splitter = MarkdownSplitter()\n        &gt;&gt;&gt; splitter.estimate_tokens(\"Hello world this is a test\")\n        8\n    \"\"\"\n    if not text.strip():\n        return 0\n\n    # Simple heuristic: split on whitespace and punctuation\n    words = len(text.split())\n\n    # Average English token/word ratio is ~1.3\n    # (accounting for subword tokenization)\n    return int(words * 1.3)\n</code></pre>"},{"location":"api/core/splitter/#markdown_reallocator.core.splitter.MarkdownSplitter.split","title":"split","text":"<pre><code>split(markdown: str) -&gt; list[Chunk]\n</code></pre> <p>Split markdown document into semantic chunks.</p> <p>Parameters:</p> Name Type Description Default <code>markdown</code> <code>str</code> <p>Markdown document to split</p> required <p>Returns:</p> Type Description <code>list[Chunk]</code> <p>List of Chunk objects with metadata</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If markdown is empty or splitting fails</p> Source code in <code>markdown_reallocator/core/splitter.py</code> <pre><code>def split(self, markdown: str) -&gt; list[Chunk]:\n    \"\"\"Split markdown document into semantic chunks.\n\n    Args:\n        markdown: Markdown document to split\n\n    Returns:\n        List of Chunk objects with metadata\n\n    Raises:\n        ValueError: If markdown is empty or splitting fails\n    \"\"\"\n    if not markdown.strip():\n        raise ValueError(\"Cannot split empty markdown\")\n\n    try:\n        # Use LangChain splitter\n        langchain_chunks = self._splitter.split_text(markdown)\n\n        if not langchain_chunks:\n            logger.warning(\"LangChain splitter returned no chunks\")\n            # Return single chunk for entire document\n            metadata = ChunkMetadata(\n                original_position=0,\n                token_count=self.estimate_tokens(markdown)\n            )\n            chunk_id = self._generate_chunk_id(markdown, 0)\n            return [Chunk(\n                chunk_id=chunk_id,\n                content=markdown,\n                metadata=metadata,\n            )]\n\n        chunks: list[Chunk] = []\n\n        for position, lc_chunk in enumerate(langchain_chunks):\n            # Extract content and metadata\n            content = lc_chunk.page_content\n\n            # Skip empty chunks\n            if not content.strip():\n                continue\n\n            # Extract metadata\n            metadata = self._extract_metadata(\n                lc_chunk.metadata,\n                position,\n                content\n            )\n\n            # Check token limit\n            if metadata.token_count &gt; self.max_tokens_per_chunk:\n                logger.warning(\n                    f\"Chunk at position {position} exceeds max tokens \"\n                    f\"({metadata.token_count} &gt; {self.max_tokens_per_chunk})\"\n                )\n\n            # Generate chunk ID\n            chunk_id = self._generate_chunk_id(content, position)\n\n            # Create chunk\n            chunk = Chunk(\n                chunk_id=chunk_id,\n                content=content,\n                metadata=metadata,\n            )\n\n            chunks.append(chunk)\n\n        if not chunks:\n            raise ValueError(\"No valid chunks created after splitting\")\n\n        return chunks\n\n    except Exception as e:\n        # Log and re-raise with context\n        logger.error(f\"Failed to split markdown: {e}\")\n        raise\n</code></pre>"},{"location":"api/core/splitter/#markdown_reallocator.core.splitter.MarkdownSplitter.split_with_metadata","title":"split_with_metadata","text":"<pre><code>split_with_metadata(markdown: str) -&gt; tuple[list[Chunk], dict[str, Any]]\n</code></pre> <p>Split markdown and return both chunks and splitting metadata.</p> <p>Parameters:</p> Name Type Description Default <code>markdown</code> <code>str</code> <p>Markdown document to split</p> required <p>Returns:</p> Type Description <code>list[Chunk]</code> <p>Tuple of (chunks, metadata_dict) where metadata_dict contains:</p> <code>dict[str, Any]</code> <ul> <li>total_chunks: Number of chunks created</li> </ul> <code>tuple[list[Chunk], dict[str, Any]]</code> <ul> <li>total_tokens: Sum of all token counts</li> </ul> <code>tuple[list[Chunk], dict[str, Any]]</code> <ul> <li>max_chunk_tokens: Maximum tokens in any chunk</li> </ul> <code>tuple[list[Chunk], dict[str, Any]]</code> <ul> <li>headers_used: Set of header levels found</li> </ul> <p>Examples:</p> <pre><code>&gt;&gt;&gt; splitter = MarkdownSplitter()\n&gt;&gt;&gt; markdown = \"# Title\\n\\n Content\"\n&gt;&gt;&gt; chunks, meta = splitter.split_with_metadata(markdown)\n&gt;&gt;&gt; meta['total_chunks']\n1\n</code></pre> Source code in <code>markdown_reallocator/core/splitter.py</code> <pre><code>def split_with_metadata(self, markdown: str) -&gt; tuple[list[Chunk], dict[str, Any]]:\n    \"\"\"Split markdown and return both chunks and splitting metadata.\n\n    Args:\n        markdown: Markdown document to split\n\n    Returns:\n        Tuple of (chunks, metadata_dict) where metadata_dict contains:\n        - total_chunks: Number of chunks created\n        - total_tokens: Sum of all token counts\n        - max_chunk_tokens: Maximum tokens in any chunk\n        - headers_used: Set of header levels found\n\n    Examples:\n        &gt;&gt;&gt; splitter = MarkdownSplitter()\n        &gt;&gt;&gt; markdown = \"# Title\\\\n\\\\n Content\"\n        &gt;&gt;&gt; chunks, meta = splitter.split_with_metadata(markdown)\n        &gt;&gt;&gt; meta['total_chunks']\n        1\n    \"\"\"\n    chunks = self.split(markdown)\n\n    # Collect statistics\n    total_tokens = sum(c.metadata.token_count for c in chunks)\n    max_chunk_tokens = max(c.metadata.token_count for c in chunks) if chunks else 0\n\n    # Determine which header levels were used\n    headers_used = set()\n    for chunk in chunks:\n        if chunk.metadata.h1:\n            headers_used.add(\"h1\")\n        if chunk.metadata.h2:\n            headers_used.add(\"h2\")\n        if chunk.metadata.h3:\n            headers_used.add(\"h3\")\n\n    metadata = {\n        \"total_chunks\": len(chunks),\n        \"total_tokens\": total_tokens,\n        \"max_chunk_tokens\": max_chunk_tokens,\n        \"headers_used\": sorted(headers_used),\n    }\n\n    return chunks, metadata\n</code></pre>"},{"location":"api/models/chunk/","title":"Chunk","text":""},{"location":"api/models/chunk/#markdown_reallocator.models.chunk","title":"markdown_reallocator.models.chunk","text":"<p>Data models for markdown chunks and metadata.</p>"},{"location":"api/models/chunk/#markdown_reallocator.models.chunk.Chunk","title":"Chunk  <code>dataclass</code>","text":"<pre><code>Chunk(chunk_id: str, content: str, metadata: ChunkMetadata, embedding: ndarray | None = None)\n</code></pre> <p>A semantic chunk of markdown content.</p> <p>Attributes:</p> Name Type Description <code>chunk_id</code> <code>str</code> <p>Unique identifier for this chunk</p> <code>content</code> <code>str</code> <p>Markdown content of the chunk</p> <code>metadata</code> <code>ChunkMetadata</code> <p>Metadata about headers and position</p> <code>embedding</code> <code>ndarray | None</code> <p>Optional embedding vector (lazy-loaded)</p>"},{"location":"api/models/chunk/#markdown_reallocator.models.chunk.Chunk.to_dict","title":"to_dict","text":"<pre><code>to_dict(include_embedding: bool = False) -&gt; dict[str, object]\n</code></pre> <p>Convert to dictionary for serialization.</p> <p>Parameters:</p> Name Type Description Default <code>include_embedding</code> <code>bool</code> <p>Whether to include embedding vector. If True, embedding is converted to list.</p> <code>False</code> <p>Returns:</p> Type Description <code>dict[str, object]</code> <p>Dictionary representation of the chunk.</p> Source code in <code>markdown_reallocator/models/chunk.py</code> <pre><code>def to_dict(self, include_embedding: bool = False) -&gt; dict[str, object]:\n    \"\"\"Convert to dictionary for serialization.\n\n    Args:\n        include_embedding: Whether to include embedding vector.\n            If True, embedding is converted to list.\n\n    Returns:\n        Dictionary representation of the chunk.\n    \"\"\"\n    data: dict[str, object] = {\n        \"chunk_id\": self.chunk_id,\n        \"content\": self.content,\n        \"metadata\": self.metadata.to_dict(),\n    }\n    if include_embedding and self.embedding is not None:\n        data[\"embedding\"] = self.embedding.tolist()\n    return data\n</code></pre>"},{"location":"api/models/chunk/#markdown_reallocator.models.chunk.Chunk.from_dict","title":"from_dict  <code>classmethod</code>","text":"<pre><code>from_dict(data: dict[str, object]) -&gt; Chunk\n</code></pre> <p>Create chunk from dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, object]</code> <p>Dictionary with chunk_id, content, metadata, and optional embedding.</p> required <p>Returns:</p> Type Description <code>Chunk</code> <p>Chunk instance.</p> Source code in <code>markdown_reallocator/models/chunk.py</code> <pre><code>@classmethod\ndef from_dict(cls, data: dict[str, object]) -&gt; \"Chunk\":\n    \"\"\"Create chunk from dictionary.\n\n    Args:\n        data: Dictionary with chunk_id, content, metadata, and optional embedding.\n\n    Returns:\n        Chunk instance.\n    \"\"\"\n    metadata_dict = data.get(\"metadata\")\n    if not isinstance(metadata_dict, dict):\n        raise ValueError(\"metadata must be a dict\")\n\n    metadata = ChunkMetadata.from_dict(metadata_dict)\n\n    embedding_data = data.get(\"embedding\")\n    embedding: np.ndarray | None = None\n    if embedding_data is not None:\n        embedding = np.array(embedding_data, dtype=np.float32)\n\n    return cls(\n        chunk_id=str(data[\"chunk_id\"]),\n        content=str(data[\"content\"]),\n        metadata=metadata,\n        embedding=embedding,\n    )\n</code></pre>"},{"location":"api/models/chunk/#markdown_reallocator.models.chunk.ChunkMetadata","title":"ChunkMetadata  <code>dataclass</code>","text":"<pre><code>ChunkMetadata(h1: str | None = None, h2: str | None = None, h3: str | None = None, original_position: int = 0, token_count: int = 0)\n</code></pre> <p>Metadata for a markdown chunk.</p> <p>Attributes:</p> Name Type Description <code>h1</code> <code>str | None</code> <p>Level 1 header (# Title) if present</p> <code>h2</code> <code>str | None</code> <p>Level 2 header (## Title) if present</p> <code>h3</code> <code>str | None</code> <p>Level 3 header (### Title) if present</p> <code>original_position</code> <code>int</code> <p>Position in original document (0-indexed)</p> <code>token_count</code> <code>int</code> <p>Estimated token count for this chunk</p>"},{"location":"api/models/chunk/#markdown_reallocator.models.chunk.ChunkMetadata.header_path","title":"header_path","text":"<pre><code>header_path() -&gt; str\n</code></pre> <p>Return header hierarchy as string.</p> <p>Returns:</p> Type Description <code>str</code> <p>Header path in format \"H1 &gt; H2 &gt; H3\", omitting None headers.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; meta = ChunkMetadata(h1=\"Intro\", h2=\"Setup\")\n&gt;&gt;&gt; meta.header_path()\n'Intro &gt; Setup'\n</code></pre> Source code in <code>markdown_reallocator/models/chunk.py</code> <pre><code>def header_path(self) -&gt; str:\n    \"\"\"Return header hierarchy as string.\n\n    Returns:\n        Header path in format \"H1 &gt; H2 &gt; H3\", omitting None headers.\n\n    Examples:\n        &gt;&gt;&gt; meta = ChunkMetadata(h1=\"Intro\", h2=\"Setup\")\n        &gt;&gt;&gt; meta.header_path()\n        'Intro &gt; Setup'\n    \"\"\"\n    headers = [h for h in [self.h1, self.h2, self.h3] if h is not None]\n    return \" &gt; \".join(headers) if headers else \"\"\n</code></pre>"},{"location":"api/models/chunk/#markdown_reallocator.models.chunk.ChunkMetadata.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, str | int | None]\n</code></pre> <p>Convert to dictionary for serialization.</p> Source code in <code>markdown_reallocator/models/chunk.py</code> <pre><code>def to_dict(self) -&gt; dict[str, str | int | None]:\n    \"\"\"Convert to dictionary for serialization.\"\"\"\n    return {\n        \"h1\": self.h1,\n        \"h2\": self.h2,\n        \"h3\": self.h3,\n        \"original_position\": self.original_position,\n        \"token_count\": self.token_count,\n    }\n</code></pre>"},{"location":"api/models/chunk/#markdown_reallocator.models.chunk.ChunkMetadata.from_dict","title":"from_dict  <code>classmethod</code>","text":"<pre><code>from_dict(data: dict[str, str | int | None]) -&gt; ChunkMetadata\n</code></pre> <p>Create from dictionary.</p> Source code in <code>markdown_reallocator/models/chunk.py</code> <pre><code>@classmethod\ndef from_dict(cls, data: dict[str, str | int | None]) -&gt; \"ChunkMetadata\":\n    \"\"\"Create from dictionary.\"\"\"\n    # Extract values with proper type checking\n    h1_val = data.get(\"h1\")\n    h2_val = data.get(\"h2\")\n    h3_val = data.get(\"h3\")\n    pos_val = data.get(\"original_position\")\n    tok_val = data.get(\"token_count\")\n\n    return cls(\n        h1=h1_val if isinstance(h1_val, str) else None,\n        h2=h2_val if isinstance(h2_val, str) else None,\n        h3=h3_val if isinstance(h3_val, str) else None,\n        original_position=int(pos_val) if pos_val is not None else 0,\n        token_count=int(tok_val) if tok_val is not None else 0,\n    )\n</code></pre>"},{"location":"api/models/embedding/","title":"Embedding","text":""},{"location":"api/models/embedding/#markdown_reallocator.models.embedding","title":"markdown_reallocator.models.embedding","text":"<p>Data model for embeddings.</p>"},{"location":"api/models/embedding/#markdown_reallocator.models.embedding.Embedding","title":"Embedding  <code>dataclass</code>","text":"<pre><code>Embedding(vector: ndarray, text_hash: str, model_name: str)\n</code></pre> <p>An embedding vector with metadata.</p> <p>Attributes:</p> Name Type Description <code>vector</code> <code>ndarray</code> <p>The embedding vector (typically 384-dim for embeddinggemma)</p> <code>text_hash</code> <code>str</code> <p>SHA256 hash of the text that was embedded</p> <code>model_name</code> <code>str</code> <p>Name of the model used (e.g., \"embeddinggemma\")</p>"},{"location":"api/models/embedding/#markdown_reallocator.models.embedding.Embedding.dimension","title":"dimension  <code>property</code>","text":"<pre><code>dimension: int\n</code></pre> <p>Return the dimensionality of the embedding.</p>"},{"location":"api/models/embedding/#markdown_reallocator.models.embedding.Embedding.normalize","title":"normalize","text":"<pre><code>normalize() -&gt; Embedding\n</code></pre> <p>Return a new Embedding with normalized vector.</p> <p>Normalization preserves direction but sets magnitude to 1. Useful for cosine similarity calculations.</p> <p>Returns:</p> Type Description <code>Embedding</code> <p>New Embedding instance with normalized vector.</p> Source code in <code>markdown_reallocator/models/embedding.py</code> <pre><code>def normalize(self) -&gt; \"Embedding\":\n    \"\"\"Return a new Embedding with normalized vector.\n\n    Normalization preserves direction but sets magnitude to 1.\n    Useful for cosine similarity calculations.\n\n    Returns:\n        New Embedding instance with normalized vector.\n    \"\"\"\n    norm = np.linalg.norm(self.vector)\n    if norm == 0:\n        raise ValueError(\"Cannot normalize zero vector\")\n\n    normalized_vector = self.vector / norm\n    return Embedding(\n        vector=normalized_vector,\n        text_hash=self.text_hash,\n        model_name=self.model_name,\n    )\n</code></pre>"},{"location":"api/models/embedding/#markdown_reallocator.models.embedding.Embedding.to_bytes","title":"to_bytes","text":"<pre><code>to_bytes() -&gt; bytes\n</code></pre> <p>Serialize vector to bytes for storage.</p> <p>Returns:</p> Type Description <code>bytes</code> <p>Binary representation of the vector.</p> Source code in <code>markdown_reallocator/models/embedding.py</code> <pre><code>def to_bytes(self) -&gt; bytes:\n    \"\"\"Serialize vector to bytes for storage.\n\n    Returns:\n        Binary representation of the vector.\n    \"\"\"\n    return self.vector.tobytes()\n</code></pre>"},{"location":"api/models/embedding/#markdown_reallocator.models.embedding.Embedding.from_bytes","title":"from_bytes  <code>classmethod</code>","text":"<pre><code>from_bytes(data: bytes, text_hash: str, model_name: str, dtype: type[floating] = float32) -&gt; Embedding\n</code></pre> <p>Deserialize vector from bytes.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>bytes</code> <p>Binary representation of the vector</p> required <code>text_hash</code> <code>str</code> <p>SHA256 hash of the embedded text</p> required <code>model_name</code> <code>str</code> <p>Name of the embedding model</p> required <code>dtype</code> <code>type[floating]</code> <p>Data type of the vector (default: float32)</p> <code>float32</code> <p>Returns:</p> Type Description <code>Embedding</code> <p>Embedding instance.</p> Source code in <code>markdown_reallocator/models/embedding.py</code> <pre><code>@classmethod\ndef from_bytes(\n    cls,\n    data: bytes,\n    text_hash: str,\n    model_name: str,\n    dtype: type[np.floating] = np.float32,\n) -&gt; \"Embedding\":\n    \"\"\"Deserialize vector from bytes.\n\n    Args:\n        data: Binary representation of the vector\n        text_hash: SHA256 hash of the embedded text\n        model_name: Name of the embedding model\n        dtype: Data type of the vector (default: float32)\n\n    Returns:\n        Embedding instance.\n    \"\"\"\n    vector = np.frombuffer(data, dtype=dtype)\n    return cls(vector=vector, text_hash=text_hash, model_name=model_name)\n</code></pre>"},{"location":"api/models/embedding/#markdown_reallocator.models.embedding.Embedding.compute_text_hash","title":"compute_text_hash  <code>staticmethod</code>","text":"<pre><code>compute_text_hash(text: str) -&gt; str\n</code></pre> <p>Compute SHA256 hash of text for cache keys.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>The text to hash.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Hexadecimal SHA256 hash string.</p> Source code in <code>markdown_reallocator/models/embedding.py</code> <pre><code>@staticmethod\ndef compute_text_hash(text: str) -&gt; str:\n    \"\"\"Compute SHA256 hash of text for cache keys.\n\n    Args:\n        text: The text to hash.\n\n    Returns:\n        Hexadecimal SHA256 hash string.\n    \"\"\"\n    return hashlib.sha256(text.encode(\"utf-8\")).hexdigest()\n</code></pre>"},{"location":"api/modules/dedup/","title":"Deduplication","text":""},{"location":"api/modules/dedup/#markdown_reallocator.modules.dedup","title":"markdown_reallocator.modules.dedup","text":"<p>Module for detecting and removing duplicate chunks.</p> <p>This module provides similarity-based duplicate detection with optional LLM verification using Gemma 2B for improved precision.</p>"},{"location":"api/modules/dedup/#markdown_reallocator.modules.dedup.DeduplicationModule","title":"DeduplicationModule","text":"<pre><code>DeduplicationModule(similarity_threshold: float = 0.85, selection_strategy: str = 'first', use_llm: bool = False, dry_run: bool = False)\n</code></pre> <p>Module for detecting and removing duplicate or near-duplicate chunks.</p> <p>This module identifies duplicate chunks based on embedding similarity and optionally verifies them using Gemma 2B LLM. Supports various strategies for selecting which chunk to keep from each duplicate group.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; dedup = DeduplicationModule(similarity_threshold=0.85)\n&gt;&gt;&gt; chunks = [...]  # Chunks with embeddings\n&gt;&gt;&gt; result, report = dedup.deduplicate(chunks)\n&gt;&gt;&gt; print(f\"Removed {report.removed_count} duplicates\")\n</code></pre> <p>Initialize deduplication module.</p> <p>Parameters:</p> Name Type Description Default <code>similarity_threshold</code> <code>float</code> <p>Minimum similarity to consider chunks duplicates (0.0-1.0)</p> <code>0.85</code> <code>selection_strategy</code> <code>str</code> <p>Strategy for selecting which chunk to keep                (\"first\", \"longest\", \"best_metadata\")</p> <code>'first'</code> <code>use_llm</code> <code>bool</code> <p>Whether to use Gemma 2B for verification (requires more memory)</p> <code>False</code> <code>dry_run</code> <code>bool</code> <p>If True, only report duplicates without removing them</p> <code>False</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If threshold out of range or strategy invalid</p> Source code in <code>markdown_reallocator/modules/dedup.py</code> <pre><code>def __init__(\n    self,\n    similarity_threshold: float = 0.85,\n    selection_strategy: str = \"first\",\n    use_llm: bool = False,\n    dry_run: bool = False,\n):\n    \"\"\"Initialize deduplication module.\n\n    Args:\n        similarity_threshold: Minimum similarity to consider chunks duplicates (0.0-1.0)\n        selection_strategy: Strategy for selecting which chunk to keep\n                           (\"first\", \"longest\", \"best_metadata\")\n        use_llm: Whether to use Gemma 2B for verification (requires more memory)\n        dry_run: If True, only report duplicates without removing them\n\n    Raises:\n        ValueError: If threshold out of range or strategy invalid\n    \"\"\"\n    # Validate threshold\n    if not 0.0 &lt;= similarity_threshold &lt;= 1.0:\n        raise ValueError(\n            f\"similarity_threshold must be in [0.0, 1.0], got {similarity_threshold}\"\n        )\n\n    # Validate strategy\n    try:\n        self.selection_strategy = SelectionStrategy(selection_strategy)\n    except ValueError as e:\n        valid = [s.value for s in SelectionStrategy]\n        raise ValueError(\n            f\"Invalid selection_strategy '{selection_strategy}'. \"\n            f\"Must be one of: {valid}\"\n        ) from e\n\n    self.similarity_threshold = similarity_threshold\n    self.use_llm = use_llm\n    self.dry_run = dry_run\n\n    # LLM client (lazy-loaded)\n    self._llm_client: Any = None\n</code></pre>"},{"location":"api/modules/dedup/#markdown_reallocator.modules.dedup.DeduplicationModule.find_duplicates","title":"find_duplicates","text":"<pre><code>find_duplicates(chunks: list[Chunk]) -&gt; list[list[int]]\n</code></pre> <p>Find groups of duplicate chunks based on similarity.</p> <p>Uses transitive closure to group all related chunks together. For example, if A is similar to B, and B is similar to C, then A, B, and C form one group even if A and C aren't directly similar.</p> <p>Parameters:</p> Name Type Description Default <code>chunks</code> <code>list[Chunk]</code> <p>List of chunks with embeddings</p> required <p>Returns:</p> Type Description <code>list[list[int]]</code> <p>List of duplicate groups, where each group is a list of chunk indices</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If chunks lack embeddings</p> Source code in <code>markdown_reallocator/modules/dedup.py</code> <pre><code>def find_duplicates(self, chunks: list[Chunk]) -&gt; list[list[int]]:\n    \"\"\"Find groups of duplicate chunks based on similarity.\n\n    Uses transitive closure to group all related chunks together.\n    For example, if A is similar to B, and B is similar to C,\n    then A, B, and C form one group even if A and C aren't directly similar.\n\n    Args:\n        chunks: List of chunks with embeddings\n\n    Returns:\n        List of duplicate groups, where each group is a list of chunk indices\n\n    Raises:\n        ValueError: If chunks lack embeddings\n    \"\"\"\n    if not chunks:\n        return []\n\n    # Validate all chunks have embeddings\n    for i, chunk in enumerate(chunks):\n        if chunk.embedding is None:\n            raise ValueError(\n                f\"Chunk at index {i} (id={chunk.chunk_id}) has no embedding. \"\n                \"Run embedder first.\"\n            )\n\n    # Compute pairwise similarity matrix\n    embeddings = np.array([c.embedding for c in chunks])\n    similarity_matrix = pairwise_cosine_similarity(embeddings)\n\n    logger.debug(\n        f\"Computed similarity matrix for {len(chunks)} chunks: \"\n        f\"shape={similarity_matrix.shape}\"\n    )\n\n    # Find pairs above threshold\n    n = len(chunks)\n    duplicate_pairs: set[tuple[int, int]] = set()\n\n    for i in range(n):\n        for j in range(i + 1, n):\n            if similarity_matrix[i, j] &gt;= self.similarity_threshold:\n                duplicate_pairs.add((i, j))\n\n    logger.debug(\n        f\"Found {len(duplicate_pairs)} duplicate pairs above threshold \"\n        f\"{self.similarity_threshold}\"\n    )\n\n    # Build duplicate groups using union-find (transitive closure)\n    groups = self._build_duplicate_groups(n, duplicate_pairs)\n\n    logger.info(\n        f\"Found {len(groups)} duplicate groups from {len(chunks)} chunks\"\n    )\n\n    return groups\n</code></pre>"},{"location":"api/modules/dedup/#markdown_reallocator.modules.dedup.DeduplicationModule.deduplicate","title":"deduplicate","text":"<pre><code>deduplicate(chunks: list[Chunk]) -&gt; tuple[list[Chunk], DeduplicationReport]\n</code></pre> <p>Remove duplicate chunks from the list.</p> <p>Parameters:</p> Name Type Description Default <code>chunks</code> <code>list[Chunk]</code> <p>List of chunks with embeddings</p> required <p>Returns:</p> Type Description <code>tuple[list[Chunk], DeduplicationReport]</code> <p>Tuple of (deduplicated_chunks, report)</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If chunks lack embeddings</p> Source code in <code>markdown_reallocator/modules/dedup.py</code> <pre><code>def deduplicate(\n    self, chunks: list[Chunk]\n) -&gt; tuple[list[Chunk], DeduplicationReport]:\n    \"\"\"Remove duplicate chunks from the list.\n\n    Args:\n        chunks: List of chunks with embeddings\n\n    Returns:\n        Tuple of (deduplicated_chunks, report)\n\n    Raises:\n        ValueError: If chunks lack embeddings\n    \"\"\"\n    if not chunks:\n        return [], DeduplicationReport(\n            original_count=0,\n            deduplicated_count=0,\n            removed_count=0,\n            duplicate_groups=[],\n            removed_chunk_ids=[],\n            kept_chunk_ids=[],\n        )\n\n    original_count = len(chunks)\n\n    # Find duplicate groups\n    duplicate_groups = self.find_duplicates(chunks)\n\n    if not duplicate_groups:\n        logger.info(\"No duplicates found\")\n        return chunks.copy(), DeduplicationReport(\n            original_count=original_count,\n            deduplicated_count=original_count,\n            removed_count=0,\n            duplicate_groups=[],\n            removed_chunk_ids=[],\n            kept_chunk_ids=[c.chunk_id for c in chunks],\n        )\n\n    # Determine which chunks to keep/remove\n    chunks_to_keep: set[int] = set(range(len(chunks)))\n    kept_from_groups: list[int] = []\n\n    for group in duplicate_groups:\n        # Select best chunk from group\n        best_idx = self._select_best_chunk(chunks, group)\n        kept_from_groups.append(best_idx)\n\n        # Mark others for removal\n        for idx in group:\n            if idx != best_idx:\n                chunks_to_keep.discard(idx)\n\n    # Build result\n    if self.dry_run:\n        # Dry run: return original chunks but with report\n        deduplicated_chunks = chunks.copy()\n    else:\n        # Actually remove duplicates\n        deduplicated_chunks = [\n            chunks[i] for i in sorted(chunks_to_keep)\n        ]\n\n    # Build report\n    removed_indices = set(range(len(chunks))) - chunks_to_keep\n    report = DeduplicationReport(\n        original_count=original_count,\n        deduplicated_count=len(deduplicated_chunks),\n        removed_count=len(removed_indices),\n        duplicate_groups=[\n            [chunks[i].chunk_id for i in group]\n            for group in duplicate_groups\n        ],\n        removed_chunk_ids=[chunks[i].chunk_id for i in sorted(removed_indices)],\n        kept_chunk_ids=[c.chunk_id for c in deduplicated_chunks],\n    )\n\n    logger.info(\n        f\"Deduplication complete: {original_count} \u2192 {len(deduplicated_chunks)} \"\n        f\"({report.reduction_percentage:.1f}% reduction)\"\n    )\n\n    return deduplicated_chunks, report\n</code></pre>"},{"location":"api/modules/dedup/#markdown_reallocator.modules.dedup.DeduplicationModule.format_report","title":"format_report","text":"<pre><code>format_report(report: DeduplicationReport) -&gt; str\n</code></pre> <p>Format deduplication report as human-readable text.</p> <p>Parameters:</p> Name Type Description Default <code>report</code> <code>DeduplicationReport</code> <p>Deduplication report</p> required <p>Returns:</p> Type Description <code>str</code> <p>Formatted report string</p> Source code in <code>markdown_reallocator/modules/dedup.py</code> <pre><code>def format_report(self, report: DeduplicationReport) -&gt; str:\n    \"\"\"Format deduplication report as human-readable text.\n\n    Args:\n        report: Deduplication report\n\n    Returns:\n        Formatted report string\n    \"\"\"\n    lines = [\"Deduplication Report\", \"=\" * 50]\n\n    # Summary\n    lines.append(f\"\\nOriginal chunks: {report.original_count}\")\n    lines.append(f\"After deduplication: {report.deduplicated_count}\")\n    lines.append(f\"Removed: {report.removed_count} ({report.reduction_percentage:.1f}%)\")\n\n    # Duplicate groups\n    if report.duplicate_groups:\n        lines.append(f\"\\nDuplicate groups: {len(report.duplicate_groups)}\")\n        for i, group in enumerate(report.duplicate_groups, 1):\n            lines.append(f\"\\n  Group {i}: {len(group)} chunks\")\n            for chunk_id in group[:3]:  # Show first 3\n                lines.append(f\"    - {chunk_id}\")\n            if len(group) &gt; 3:\n                lines.append(f\"    ... and {len(group) - 3} more\")\n\n    # Removed chunks\n    if report.removed_chunk_ids:\n        lines.append(f\"\\nRemoved chunk IDs ({len(report.removed_chunk_ids)}):\")\n        for chunk_id in report.removed_chunk_ids[:10]:  # Show first 10\n            lines.append(f\"  - {chunk_id}\")\n        if len(report.removed_chunk_ids) &gt; 10:\n            lines.append(f\"  ... and {len(report.removed_chunk_ids) - 10} more\")\n\n    return \"\\n\".join(lines)\n</code></pre>"},{"location":"api/modules/dedup/#markdown_reallocator.modules.dedup.DeduplicationReport","title":"DeduplicationReport  <code>dataclass</code>","text":"<pre><code>DeduplicationReport(original_count: int, deduplicated_count: int, removed_count: int, duplicate_groups: list[list[str]], removed_chunk_ids: list[str], kept_chunk_ids: list[str])\n</code></pre> <p>Report of deduplication operation.</p>"},{"location":"api/modules/dedup/#markdown_reallocator.modules.dedup.DeduplicationReport.reduction_percentage","title":"reduction_percentage  <code>property</code>","text":"<pre><code>reduction_percentage: float\n</code></pre> <p>Calculate percentage of chunks removed.</p>"},{"location":"api/modules/reorder/","title":"Reorder","text":""},{"location":"api/modules/reorder/#markdown_reallocator.modules.reorder","title":"markdown_reallocator.modules.reorder","text":"<p>Module for reordering markdown chunks by semantic similarity.</p> <p>This module provides strategies for reorganizing document chunks based on their embedding similarity to improve document flow and coherence.</p>"},{"location":"api/modules/reorder/#markdown_reallocator.modules.reorder.ReorderModule","title":"ReorderModule","text":"<pre><code>ReorderModule(strategy: str = 'sequential', similarity_threshold: float = 0.5)\n</code></pre> <p>Module for reordering document chunks by semantic similarity.</p> <p>This module provides two strategies for reorganizing chunks: - cluster: Group similar chunks together using hierarchical clustering - sequential: Chain chunks by highest similarity to previous chunk</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; reorder = ReorderModule(strategy=\"sequential\")\n&gt;&gt;&gt; chunks = [...]  # Chunks with embeddings\n&gt;&gt;&gt; reordered = reorder.reorder(chunks)\n&gt;&gt;&gt; markdown = reorder.reconstruct_markdown(reordered)\n</code></pre> <p>Initialize reorder module.</p> <p>Parameters:</p> Name Type Description Default <code>strategy</code> <code>str</code> <p>Reordering strategy (\"cluster\" or \"sequential\")</p> <code>'sequential'</code> <code>similarity_threshold</code> <code>float</code> <p>Minimum similarity for considering chunks related (0.0-1.0)</p> <code>0.5</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If strategy is invalid or threshold out of range</p> Source code in <code>markdown_reallocator/modules/reorder.py</code> <pre><code>def __init__(\n    self,\n    strategy: str = \"sequential\",\n    similarity_threshold: float = 0.5,\n):\n    \"\"\"Initialize reorder module.\n\n    Args:\n        strategy: Reordering strategy (\"cluster\" or \"sequential\")\n        similarity_threshold: Minimum similarity for considering chunks related (0.0-1.0)\n\n    Raises:\n        ValueError: If strategy is invalid or threshold out of range\n    \"\"\"\n    # Validate strategy\n    try:\n        self.strategy = ReorderStrategy(strategy)\n    except ValueError as e:\n        valid = [s.value for s in ReorderStrategy]\n        raise ValueError(\n            f\"Invalid strategy '{strategy}'. Must be one of: {valid}\"\n        ) from e\n\n    # Validate threshold\n    if not 0.0 &lt;= similarity_threshold &lt;= 1.0:\n        raise ValueError(\n            f\"similarity_threshold must be in [0.0, 1.0], got {similarity_threshold}\"\n        )\n\n    self.similarity_threshold = similarity_threshold\n</code></pre>"},{"location":"api/modules/reorder/#markdown_reallocator.modules.reorder.ReorderModule.compute_similarity_matrix","title":"compute_similarity_matrix","text":"<pre><code>compute_similarity_matrix(chunks: list[Chunk]) -&gt; ndarray\n</code></pre> <p>Compute pairwise similarity matrix for chunks.</p> <p>Parameters:</p> Name Type Description Default <code>chunks</code> <code>list[Chunk]</code> <p>List of chunks with embeddings</p> required <p>Returns:</p> Type Description <code>ndarray</code> <p>Symmetric similarity matrix (n x n) where element [i,j] is</p> <code>ndarray</code> <p>cosine similarity between chunks[i] and chunks[j]</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If any chunk is missing embedding</p> Source code in <code>markdown_reallocator/modules/reorder.py</code> <pre><code>def compute_similarity_matrix(\n    self, chunks: list[Chunk]\n) -&gt; np.ndarray:\n    \"\"\"Compute pairwise similarity matrix for chunks.\n\n    Args:\n        chunks: List of chunks with embeddings\n\n    Returns:\n        Symmetric similarity matrix (n x n) where element [i,j] is\n        cosine similarity between chunks[i] and chunks[j]\n\n    Raises:\n        ValueError: If any chunk is missing embedding\n    \"\"\"\n    # Validate all chunks have embeddings\n    for i, chunk in enumerate(chunks):\n        if chunk.embedding is None:\n            raise ValueError(\n                f\"Chunk at index {i} (id={chunk.chunk_id}) has no embedding. \"\n                \"Run embedder first.\"\n            )\n\n    # Extract embeddings into matrix\n    embeddings = np.array([chunk.embedding for chunk in chunks])\n\n    # Compute pairwise similarities\n    logger.debug(f\"Computing similarity matrix for {len(chunks)} chunks\")\n    similarity_matrix = pairwise_cosine_similarity(embeddings)\n\n    logger.debug(\n        f\"Similarity matrix computed: shape={similarity_matrix.shape}, \"\n        f\"dtype={similarity_matrix.dtype}\"\n    )\n\n    return similarity_matrix\n</code></pre>"},{"location":"api/modules/reorder/#markdown_reallocator.modules.reorder.ReorderModule.reorder","title":"reorder","text":"<pre><code>reorder(chunks: list[Chunk]) -&gt; list[Chunk]\n</code></pre> <p>Reorder chunks using configured strategy.</p> <p>Parameters:</p> Name Type Description Default <code>chunks</code> <code>list[Chunk]</code> <p>List of chunks with embeddings</p> required <p>Returns:</p> Type Description <code>list[Chunk]</code> <p>Reordered list of chunks</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If chunks list is empty or chunks lack embeddings</p> Source code in <code>markdown_reallocator/modules/reorder.py</code> <pre><code>def reorder(self, chunks: list[Chunk]) -&gt; list[Chunk]:\n    \"\"\"Reorder chunks using configured strategy.\n\n    Args:\n        chunks: List of chunks with embeddings\n\n    Returns:\n        Reordered list of chunks\n\n    Raises:\n        ValueError: If chunks list is empty or chunks lack embeddings\n    \"\"\"\n    if not chunks:\n        raise ValueError(\"Cannot reorder empty chunk list\")\n\n    # Compute similarity matrix\n    similarity_matrix = self.compute_similarity_matrix(chunks)\n\n    # Apply strategy\n    if self.strategy == ReorderStrategy.SEQUENTIAL:\n        return self._sequential_reorder(chunks, similarity_matrix)\n    elif self.strategy == ReorderStrategy.CLUSTER:\n        return self._cluster_reorder(chunks, similarity_matrix)\n    else:\n        raise NotImplementedError(f\"Strategy {self.strategy} not implemented\")\n</code></pre>"},{"location":"api/modules/reorder/#markdown_reallocator.modules.reorder.ReorderModule.reconstruct_markdown","title":"reconstruct_markdown","text":"<pre><code>reconstruct_markdown(chunks: list[Chunk], include_comments: bool = False) -&gt; str\n</code></pre> <p>Reconstruct markdown document from reordered chunks.</p> <p>Preserves header hierarchy and ensures proper spacing between chunks. Optionally includes HTML comments showing original positions.</p> <p>Parameters:</p> Name Type Description Default <code>chunks</code> <code>list[Chunk]</code> <p>Reordered chunks</p> required <code>include_comments</code> <code>bool</code> <p>If True, add HTML comments with original positions</p> <code>False</code> <p>Returns:</p> Type Description <code>str</code> <p>Reconstructed markdown as string</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If chunks list is empty</p> Source code in <code>markdown_reallocator/modules/reorder.py</code> <pre><code>def reconstruct_markdown(\n    self, chunks: list[Chunk], include_comments: bool = False\n) -&gt; str:\n    \"\"\"Reconstruct markdown document from reordered chunks.\n\n    Preserves header hierarchy and ensures proper spacing between chunks.\n    Optionally includes HTML comments showing original positions.\n\n    Args:\n        chunks: Reordered chunks\n        include_comments: If True, add HTML comments with original positions\n\n    Returns:\n        Reconstructed markdown as string\n\n    Raises:\n        ValueError: If chunks list is empty\n    \"\"\"\n    if not chunks:\n        raise ValueError(\"Cannot reconstruct markdown from empty chunk list\")\n\n    lines = []\n\n    for i, chunk in enumerate(chunks):\n        # Add comment with original position if requested\n        if include_comments:\n            orig_pos = chunk.metadata.original_position\n            comment = f\"&lt;!-- Original position: {orig_pos}, Reordered position: {i} --&gt;\\n\"\n            lines.append(comment)\n\n        # Add chunk content\n        content = chunk.content.strip()\n        if content:\n            lines.append(content)\n\n        # Add spacing between chunks (double newline for separation)\n        if i &lt; len(chunks) - 1:\n            lines.append(\"\")  # Single empty line between chunks\n\n    # Join with newlines\n    markdown = \"\\n\".join(lines)\n\n    # Ensure single trailing newline\n    if not markdown.endswith(\"\\n\"):\n        markdown += \"\\n\"\n\n    logger.info(\n        f\"Reconstructed markdown from {len(chunks)} chunks: \"\n        f\"{len(markdown)} characters\"\n    )\n\n    return markdown\n</code></pre>"},{"location":"api/modules/search/","title":"Search","text":""},{"location":"api/modules/search/#markdown_reallocator.modules.search","title":"markdown_reallocator.modules.search","text":"<p>Module for semantic search over document chunks.</p> <p>This module provides functionality to search for relevant chunks based on natural language queries using embedding similarity.</p>"},{"location":"api/modules/search/#markdown_reallocator.modules.search.SearchModule","title":"SearchModule","text":"<pre><code>SearchModule(embedder: Embedder | None = None, top_k: int = 5, min_similarity: float = 0.5, output_format: str = 'text')\n</code></pre> <p>Module for semantic search over document chunks.</p> <p>This module enables finding relevant chunks based on natural language queries by computing cosine similarity between query embeddings and chunk embeddings.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; search = SearchModule()\n&gt;&gt;&gt; chunks = [...]  # Chunks with embeddings\n&gt;&gt;&gt; results = search.search(\"authentication methods\", chunks, top_k=5)\n&gt;&gt;&gt; for chunk, score in results:\n...     print(f\"Score: {score:.3f} - {chunk.content[:50]}\")\n</code></pre> <p>Initialize search module with configuration.</p> <p>Parameters:</p> Name Type Description Default <code>embedder</code> <code>Embedder | None</code> <p>Embedder instance for query embedding. If None, creates default.</p> <code>None</code> <code>top_k</code> <code>int</code> <p>Default number of results to return (must be &gt; 0)</p> <code>5</code> <code>min_similarity</code> <code>float</code> <p>Minimum similarity threshold (0.0-1.0)</p> <code>0.5</code> <code>output_format</code> <code>str</code> <p>Default output format (\"text\" or \"json\")</p> <code>'text'</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If top_k &lt;= 0 or min_similarity not in [0.0, 1.0]</p> Source code in <code>markdown_reallocator/modules/search.py</code> <pre><code>def __init__(\n    self,\n    embedder: Embedder | None = None,\n    top_k: int = 5,\n    min_similarity: float = 0.5,\n    output_format: str = \"text\",\n):\n    \"\"\"Initialize search module with configuration.\n\n    Args:\n        embedder: Embedder instance for query embedding. If None, creates default.\n        top_k: Default number of results to return (must be &gt; 0)\n        min_similarity: Minimum similarity threshold (0.0-1.0)\n        output_format: Default output format (\"text\" or \"json\")\n\n    Raises:\n        ValueError: If top_k &lt;= 0 or min_similarity not in [0.0, 1.0]\n    \"\"\"\n    # Validate parameters\n    if top_k &lt;= 0:\n        raise ValueError(f\"top_k must be &gt; 0, got {top_k}\")\n\n    if not 0.0 &lt;= min_similarity &lt;= 1.0:\n        raise ValueError(\n            f\"min_similarity must be in [0.0, 1.0], got {min_similarity}\"\n        )\n\n    if output_format not in (\"text\", \"json\"):\n        raise ValueError(\n            f\"output_format must be 'text' or 'json', got '{output_format}'\"\n        )\n\n    # Initialize embedder\n    self.embedder = embedder if embedder is not None else Embedder()\n\n    # Store configuration\n    self.top_k = top_k\n    self.min_similarity = min_similarity\n    self.output_format = output_format\n</code></pre>"},{"location":"api/modules/search/#markdown_reallocator.modules.search.SearchModule.search","title":"search","text":"<pre><code>search(query: str, chunks: list[Chunk], top_k: int | None = None, min_similarity: float | None = None) -&gt; list[tuple[Chunk, float]]\n</code></pre> <p>Search for chunks similar to the query.</p> <p>Embeds the query using the same model as document chunks, then computes cosine similarity between query and all chunks. Returns top-k most similar chunks with scores.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>str</code> <p>Natural language search query</p> required <code>chunks</code> <code>list[Chunk]</code> <p>List of chunks with embeddings to search</p> required <code>top_k</code> <code>int | None</code> <p>Number of results to return (overrides default if provided)</p> <code>None</code> <code>min_similarity</code> <code>float | None</code> <p>Minimum similarity threshold (overrides default)</p> <code>None</code> <p>Returns:</p> Type Description <code>list[tuple[Chunk, float]]</code> <p>List of (chunk, similarity_score) tuples, sorted by score descending</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If query is empty or chunks lack embeddings</p> Source code in <code>markdown_reallocator/modules/search.py</code> <pre><code>def search(\n    self,\n    query: str,\n    chunks: list[Chunk],\n    top_k: int | None = None,\n    min_similarity: float | None = None,\n) -&gt; list[tuple[Chunk, float]]:\n    \"\"\"Search for chunks similar to the query.\n\n    Embeds the query using the same model as document chunks, then\n    computes cosine similarity between query and all chunks. Returns\n    top-k most similar chunks with scores.\n\n    Args:\n        query: Natural language search query\n        chunks: List of chunks with embeddings to search\n        top_k: Number of results to return (overrides default if provided)\n        min_similarity: Minimum similarity threshold (overrides default)\n\n    Returns:\n        List of (chunk, similarity_score) tuples, sorted by score descending\n\n    Raises:\n        ValueError: If query is empty or chunks lack embeddings\n    \"\"\"\n    # Validate query\n    if not query or not query.strip():\n        raise ValueError(\"Query cannot be empty\")\n\n    # Validate chunks\n    if not chunks:\n        logger.warning(\"No chunks provided for search\")\n        return []\n\n    # Check all chunks have embeddings\n    for i, chunk in enumerate(chunks):\n        if chunk.embedding is None:\n            raise ValueError(\n                f\"Chunk at index {i} (id={chunk.chunk_id}) has no embedding. \"\n                \"Run embedder first.\"\n            )\n\n    # Use provided parameters or defaults\n    k = top_k if top_k is not None else self.top_k\n    threshold = (\n        min_similarity if min_similarity is not None else self.min_similarity\n    )\n\n    # Embed query\n    logger.debug(f\"Embedding query: '{query[:50]}...'\")\n\n    # Create temporary chunk for query to leverage embedder's caching\n    from markdown_reallocator.models.chunk import ChunkMetadata\n    query_chunk = Chunk(\n        chunk_id=\"__query__\",\n        content=query,\n        metadata=ChunkMetadata(original_position=0),\n    )\n\n    # Embed query (will use cache if query seen before)\n    query_chunk = self.embedder.embed_chunk(query_chunk)\n\n    if query_chunk.embedding is None:\n        raise RuntimeError(\"Failed to embed query\")\n\n    query_embedding = query_chunk.embedding\n\n    # Extract chunk embeddings into matrix\n    chunk_embeddings = np.array([c.embedding for c in chunks])\n\n    # Compute similarities between query and all chunks\n    # query_embedding shape: (dim,)\n    # chunk_embeddings shape: (n_chunks, dim)\n    # Use batch_cosine_similarity for efficient computation\n    # Result shape: (n_chunks,)\n    similarities = batch_cosine_similarity(chunk_embeddings, query_embedding)\n\n    logger.debug(\n        f\"Computed similarities for {len(chunks)} chunks: \"\n        f\"min={similarities.min():.3f}, max={similarities.max():.3f}, \"\n        f\"mean={similarities.mean():.3f}\"\n    )\n\n    # Filter by threshold\n    mask = similarities &gt;= threshold\n    filtered_indices = np.where(mask)[0]\n    filtered_similarities = similarities[filtered_indices]\n\n    logger.debug(\n        f\"Filtered to {len(filtered_indices)} chunks with similarity &gt;= {threshold}\"\n    )\n\n    if len(filtered_indices) == 0:\n        logger.info(f\"No chunks found with similarity &gt;= {threshold}\")\n        return []\n\n    # Sort by similarity (descending)\n    sorted_order = np.argsort(filtered_similarities)[::-1]\n    sorted_indices = filtered_indices[sorted_order]\n    sorted_similarities = filtered_similarities[sorted_order]\n\n    # Take top-k\n    top_indices = sorted_indices[:k]\n    top_similarities = sorted_similarities[:k]\n\n    # Build results\n    results = [\n        (chunks[idx], float(sim))\n        for idx, sim in zip(top_indices, top_similarities, strict=False)\n    ]\n\n    logger.info(\n        f\"Search complete: found {len(results)} results \"\n        f\"(top similarity: {results[0][1]:.3f})\"\n    )\n\n    return results\n</code></pre>"},{"location":"api/modules/search/#markdown_reallocator.modules.search.SearchModule.format_results","title":"format_results","text":"<pre><code>format_results(results: list[tuple[Chunk, float]], output_format: str | None = None, include_context: bool = False) -&gt; str\n</code></pre> <p>Format search results as text or JSON.</p> <p>Parameters:</p> Name Type Description Default <code>results</code> <code>list[tuple[Chunk, float]]</code> <p>List of (chunk, score) tuples from search()</p> required <code>output_format</code> <code>str | None</code> <p>Output format (\"text\" or \"json\"), uses default if None</p> <code>None</code> <code>include_context</code> <code>bool</code> <p>If True, include additional metadata context</p> <code>False</code> <p>Returns:</p> Type Description <code>str</code> <p>Formatted results as string</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If output_format is invalid</p> Source code in <code>markdown_reallocator/modules/search.py</code> <pre><code>def format_results(\n    self,\n    results: list[tuple[Chunk, float]],\n    output_format: str | None = None,\n    include_context: bool = False,\n) -&gt; str:\n    \"\"\"Format search results as text or JSON.\n\n    Args:\n        results: List of (chunk, score) tuples from search()\n        output_format: Output format (\"text\" or \"json\"), uses default if None\n        include_context: If True, include additional metadata context\n\n    Returns:\n        Formatted results as string\n\n    Raises:\n        ValueError: If output_format is invalid\n    \"\"\"\n    # Use provided format or default\n    fmt = output_format if output_format is not None else self.output_format\n\n    if fmt not in (\"text\", \"json\"):\n        raise ValueError(f\"output_format must be 'text' or 'json', got '{fmt}'\")\n\n    # Handle empty results\n    if not results:\n        if fmt == \"text\":\n            return \"No results found.\\n\"\n        else:\n            return json.dumps({\"results\": [], \"count\": 0}, indent=2)\n\n    # Format based on output type\n    if fmt == \"text\":\n        return self._format_text(results, include_context)\n    else:\n        return self._format_json(results, include_context)\n</code></pre>"},{"location":"api/utils/cache/","title":"Cache","text":""},{"location":"api/utils/cache/#markdown_reallocator.utils.cache","title":"markdown_reallocator.utils.cache","text":"<p>LRU cache implementation for embedding storage.</p> <p>This module provides an in-memory LRU (Least Recently Used) cache for storing embeddings. The cache is designed to be stateful but thread-safe, with configurable size limits and statistics tracking.</p>"},{"location":"api/utils/cache/#markdown_reallocator.utils.cache.LRUCache","title":"LRUCache","text":"<pre><code>LRUCache(max_size: int = 1000)\n</code></pre> <p>Thread-safe LRU cache for storing embeddings.</p> <p>This cache uses an OrderedDict to maintain insertion order and implements LRU (Least Recently Used) eviction policy. When the cache reaches max_size, the least recently used item is evicted.</p> <p>Attributes:</p> Name Type Description <code>max_size</code> <p>Maximum number of items to store</p> <code>stats</code> <code>CacheStatistics</code> <p>Cache statistics tracker</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; cache = LRUCache(max_size=100)\n&gt;&gt;&gt; embedding = np.array([0.1, 0.2, 0.3])\n&gt;&gt;&gt; cache.put(\"text_hash_123\", embedding)\n&gt;&gt;&gt; cached = cache.get(\"text_hash_123\")\n&gt;&gt;&gt; np.array_equal(cached, embedding)\nTrue\n&gt;&gt;&gt; cache.stats.hit_rate\n1.0\n</code></pre> <p>Initialize LRU cache.</p> <p>Parameters:</p> Name Type Description Default <code>max_size</code> <code>int</code> <p>Maximum number of items to cache (default: 1000)</p> <code>1000</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If max_size is less than 1</p> Source code in <code>markdown_reallocator/utils/cache.py</code> <pre><code>def __init__(self, max_size: int = 1000):\n    \"\"\"Initialize LRU cache.\n\n    Args:\n        max_size: Maximum number of items to cache (default: 1000)\n\n    Raises:\n        ValueError: If max_size is less than 1\n    \"\"\"\n    if max_size &lt; 1:\n        raise ValueError(f\"max_size must be at least 1, got {max_size}\")\n\n    self._cache: OrderedDict[str, np.ndarray] = OrderedDict()\n    self._max_size = max_size\n    self._stats = CacheStatistics(max_size=max_size)\n</code></pre>"},{"location":"api/utils/cache/#markdown_reallocator.utils.cache.LRUCache.stats","title":"stats  <code>property</code>","text":"<pre><code>stats: CacheStatistics\n</code></pre> <p>Get cache statistics.</p> <p>Returns:</p> Type Description <code>CacheStatistics</code> <p>Current cache statistics</p>"},{"location":"api/utils/cache/#markdown_reallocator.utils.cache.LRUCache.get","title":"get","text":"<pre><code>get(key: str) -&gt; ndarray | None\n</code></pre> <p>Retrieve item from cache.</p> <p>If item exists, it's moved to the end (most recently used). Updates cache statistics.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Cache key (typically text hash)</p> required <p>Returns:</p> Type Description <code>ndarray | None</code> <p>Cached numpy array if found, None otherwise</p> Source code in <code>markdown_reallocator/utils/cache.py</code> <pre><code>def get(self, key: str) -&gt; np.ndarray | None:\n    \"\"\"Retrieve item from cache.\n\n    If item exists, it's moved to the end (most recently used).\n    Updates cache statistics.\n\n    Args:\n        key: Cache key (typically text hash)\n\n    Returns:\n        Cached numpy array if found, None otherwise\n    \"\"\"\n    if key in self._cache:\n        # Move to end (mark as recently used)\n        self._cache.move_to_end(key)\n        self._stats.hits += 1\n        return self._cache[key]\n\n    self._stats.misses += 1\n    return None\n</code></pre>"},{"location":"api/utils/cache/#markdown_reallocator.utils.cache.LRUCache.put","title":"put","text":"<pre><code>put(key: str, value: ndarray) -&gt; None\n</code></pre> <p>Store item in cache.</p> <p>If cache is full, evicts least recently used item. If key already exists, updates value and moves to end.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Cache key (typically text hash)</p> required <code>value</code> <code>ndarray</code> <p>Numpy array to cache (typically embedding vector)</p> required <p>Raises:</p> Type Description <code>TypeError</code> <p>If value is not a numpy array</p> Source code in <code>markdown_reallocator/utils/cache.py</code> <pre><code>def put(self, key: str, value: np.ndarray) -&gt; None:\n    \"\"\"Store item in cache.\n\n    If cache is full, evicts least recently used item.\n    If key already exists, updates value and moves to end.\n\n    Args:\n        key: Cache key (typically text hash)\n        value: Numpy array to cache (typically embedding vector)\n\n    Raises:\n        TypeError: If value is not a numpy array\n    \"\"\"\n    if not isinstance(value, np.ndarray):\n        raise TypeError(f\"value must be numpy array, got {type(value).__name__}\")\n\n    # If key exists, remove it first (will be re-added at end)\n    if key in self._cache:\n        del self._cache[key]\n\n    # Add new item\n    self._cache[key] = value\n\n    # Evict oldest if over capacity\n    if len(self._cache) &gt; self._max_size:\n        # Remove first item (least recently used)\n        self._cache.popitem(last=False)\n        self._stats.evictions += 1\n</code></pre>"},{"location":"api/utils/cache/#markdown_reallocator.utils.cache.LRUCache.clear","title":"clear","text":"<pre><code>clear() -&gt; None\n</code></pre> <p>Clear all items from cache.</p> <p>Resets statistics except for max_size.</p> Source code in <code>markdown_reallocator/utils/cache.py</code> <pre><code>def clear(self) -&gt; None:\n    \"\"\"Clear all items from cache.\n\n    Resets statistics except for max_size.\n    \"\"\"\n    self._cache.clear()\n    self._stats = CacheStatistics(max_size=self._max_size)\n</code></pre>"},{"location":"api/utils/cache/#markdown_reallocator.utils.cache.EmbeddingCache","title":"EmbeddingCache","text":"<pre><code>EmbeddingCache(max_size: int = 1000)\n</code></pre> <p>High-level cache specifically for embeddings with text hash keys.</p> <p>This is a wrapper around LRUCache with additional utilities for embedding-specific operations like computing cache keys from text.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; from markdown_reallocator.models import Embedding\n&gt;&gt;&gt; cache = EmbeddingCache(max_size=100)\n&gt;&gt;&gt; text = \"Hello, world!\"\n&gt;&gt;&gt; embedding_vector = np.array([0.1, 0.2, 0.3], dtype=np.float32)\n&gt;&gt;&gt; text_hash = Embedding.compute_text_hash(text)\n&gt;&gt;&gt; cache.put(text_hash, \"embeddinggemma\", embedding_vector)\n&gt;&gt;&gt; cached = cache.get(text_hash, \"embeddinggemma\")\n&gt;&gt;&gt; np.array_equal(cached, embedding_vector)\nTrue\n</code></pre> <p>Initialize embedding cache.</p> <p>Parameters:</p> Name Type Description Default <code>max_size</code> <code>int</code> <p>Maximum number of embeddings to cache</p> <code>1000</code> Source code in <code>markdown_reallocator/utils/cache.py</code> <pre><code>def __init__(self, max_size: int = 1000):\n    \"\"\"Initialize embedding cache.\n\n    Args:\n        max_size: Maximum number of embeddings to cache\n    \"\"\"\n    self._cache = LRUCache(max_size=max_size)\n</code></pre>"},{"location":"api/utils/cache/#markdown_reallocator.utils.cache.EmbeddingCache.stats","title":"stats  <code>property</code>","text":"<pre><code>stats: CacheStatistics\n</code></pre> <p>Get cache statistics.</p>"},{"location":"api/utils/cache/#markdown_reallocator.utils.cache.EmbeddingCache.get","title":"get","text":"<pre><code>get(text_hash: str, model_name: str) -&gt; ndarray | None\n</code></pre> <p>Retrieve embedding from cache.</p> <p>Parameters:</p> Name Type Description Default <code>text_hash</code> <code>str</code> <p>SHA256 hash of text</p> required <code>model_name</code> <code>str</code> <p>Name of embedding model used</p> required <p>Returns:</p> Type Description <code>ndarray | None</code> <p>Cached embedding vector if found, None otherwise</p> Source code in <code>markdown_reallocator/utils/cache.py</code> <pre><code>def get(self, text_hash: str, model_name: str) -&gt; np.ndarray | None:\n    \"\"\"Retrieve embedding from cache.\n\n    Args:\n        text_hash: SHA256 hash of text\n        model_name: Name of embedding model used\n\n    Returns:\n        Cached embedding vector if found, None otherwise\n    \"\"\"\n    key = self._make_key(text_hash, model_name)\n    return self._cache.get(key)\n</code></pre>"},{"location":"api/utils/cache/#markdown_reallocator.utils.cache.EmbeddingCache.put","title":"put","text":"<pre><code>put(text_hash: str, model_name: str, embedding: ndarray) -&gt; None\n</code></pre> <p>Store embedding in cache.</p> <p>Parameters:</p> Name Type Description Default <code>text_hash</code> <code>str</code> <p>SHA256 hash of text</p> required <code>model_name</code> <code>str</code> <p>Name of embedding model used</p> required <code>embedding</code> <code>ndarray</code> <p>Embedding vector to cache</p> required Source code in <code>markdown_reallocator/utils/cache.py</code> <pre><code>def put(self, text_hash: str, model_name: str, embedding: np.ndarray) -&gt; None:\n    \"\"\"Store embedding in cache.\n\n    Args:\n        text_hash: SHA256 hash of text\n        model_name: Name of embedding model used\n        embedding: Embedding vector to cache\n    \"\"\"\n    key = self._make_key(text_hash, model_name)\n    self._cache.put(key, embedding)\n</code></pre>"},{"location":"api/utils/cache/#markdown_reallocator.utils.cache.EmbeddingCache.contains","title":"contains","text":"<pre><code>contains(text_hash: str, model_name: str) -&gt; bool\n</code></pre> <p>Check if embedding exists in cache.</p> <p>Parameters:</p> Name Type Description Default <code>text_hash</code> <code>str</code> <p>SHA256 hash of text</p> required <code>model_name</code> <code>str</code> <p>Name of embedding model used</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if embedding is cached, False otherwise</p> Source code in <code>markdown_reallocator/utils/cache.py</code> <pre><code>def contains(self, text_hash: str, model_name: str) -&gt; bool:\n    \"\"\"Check if embedding exists in cache.\n\n    Args:\n        text_hash: SHA256 hash of text\n        model_name: Name of embedding model used\n\n    Returns:\n        True if embedding is cached, False otherwise\n    \"\"\"\n    key = self._make_key(text_hash, model_name)\n    return key in self._cache\n</code></pre>"},{"location":"api/utils/cache/#markdown_reallocator.utils.cache.EmbeddingCache.clear","title":"clear","text":"<pre><code>clear() -&gt; None\n</code></pre> <p>Clear all cached embeddings.</p> Source code in <code>markdown_reallocator/utils/cache.py</code> <pre><code>def clear(self) -&gt; None:\n    \"\"\"Clear all cached embeddings.\"\"\"\n    self._cache.clear()\n</code></pre>"},{"location":"api/utils/memory/","title":"Memory","text":""},{"location":"api/utils/memory/#markdown_reallocator.utils.memory","title":"markdown_reallocator.utils.memory","text":"<p>Memory monitoring utilities for GPU and system memory.</p> <p>This module provides utilities for monitoring GPU and system memory usage, with graceful fallback when GPU is not available. Designed to work on resource-constrained hardware and provide warnings when memory usage is high.</p>"},{"location":"api/utils/similarity/","title":"Similarity","text":""},{"location":"api/utils/similarity/#markdown_reallocator.utils.similarity","title":"markdown_reallocator.utils.similarity","text":"<p>Similarity computation utilities for embeddings.</p> <p>This module provides efficient cosine similarity calculations optimized for numpy arrays. All functions are stateless and use vectorized operations for performance on CPUs with SSE4.2 support (Westmere and newer).</p>"},{"location":"api/utils/similarity/#markdown_reallocator.utils.similarity.cosine_similarity","title":"cosine_similarity","text":"<pre><code>cosine_similarity(v1: ndarray, v2: ndarray) -&gt; float\n</code></pre> <p>Compute cosine similarity between two vectors.</p> <p>Cosine similarity is defined as: dot(v1, v2) / (||v1|| * ||v2||) Returns a value in [-1, 1] where: - 1: vectors point in same direction (identical) - 0: vectors are orthogonal (unrelated) - -1: vectors point in opposite directions</p> <p>Parameters:</p> Name Type Description Default <code>v1</code> <code>ndarray</code> <p>First vector (1D numpy array)</p> required <code>v2</code> <code>ndarray</code> <p>Second vector (1D numpy array, same dimension as v1)</p> required <p>Returns:</p> Type Description <code>float</code> <p>Cosine similarity score as float</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If vectors have different dimensions or are not 1D</p> <code>ValueError</code> <p>If either vector has zero norm (undefined similarity)</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import numpy as np\n&gt;&gt;&gt; v1 = np.array([1, 0, 0])\n&gt;&gt;&gt; v2 = np.array([1, 0, 0])\n&gt;&gt;&gt; cosine_similarity(v1, v2)\n1.0\n&gt;&gt;&gt; v3 = np.array([0, 1, 0])\n&gt;&gt;&gt; cosine_similarity(v1, v3)\n0.0\n</code></pre> Source code in <code>markdown_reallocator/utils/similarity.py</code> <pre><code>def cosine_similarity(v1: np.ndarray, v2: np.ndarray) -&gt; float:\n    \"\"\"Compute cosine similarity between two vectors.\n\n    Cosine similarity is defined as: dot(v1, v2) / (||v1|| * ||v2||)\n    Returns a value in [-1, 1] where:\n    - 1: vectors point in same direction (identical)\n    - 0: vectors are orthogonal (unrelated)\n    - -1: vectors point in opposite directions\n\n    Args:\n        v1: First vector (1D numpy array)\n        v2: Second vector (1D numpy array, same dimension as v1)\n\n    Returns:\n        Cosine similarity score as float\n\n    Raises:\n        ValueError: If vectors have different dimensions or are not 1D\n        ValueError: If either vector has zero norm (undefined similarity)\n\n    Examples:\n        &gt;&gt;&gt; import numpy as np\n        &gt;&gt;&gt; v1 = np.array([1, 0, 0])\n        &gt;&gt;&gt; v2 = np.array([1, 0, 0])\n        &gt;&gt;&gt; cosine_similarity(v1, v2)\n        1.0\n        &gt;&gt;&gt; v3 = np.array([0, 1, 0])\n        &gt;&gt;&gt; cosine_similarity(v1, v3)\n        0.0\n    \"\"\"\n    # Validate inputs\n    if v1.ndim != 1 or v2.ndim != 1:\n        raise ValueError(f\"Vectors must be 1-dimensional, got shapes {v1.shape} and {v2.shape}\")\n\n    if v1.shape[0] != v2.shape[0]:\n        raise ValueError(\n            f\"Vectors must have same dimension, got {v1.shape[0]} and {v2.shape[0]}\"\n        )\n\n    # Compute norms\n    norm1 = np.linalg.norm(v1)\n    norm2 = np.linalg.norm(v2)\n\n    # Check for zero vectors\n    if norm1 == 0 or norm2 == 0:\n        raise ValueError(\"Cannot compute similarity with zero vector\")\n\n    # Compute cosine similarity\n    # Use numpy.dot for SSE4.2 optimization\n    similarity = np.dot(v1, v2) / (norm1 * norm2)\n\n    return float(similarity)\n</code></pre>"},{"location":"architecture/","title":"Architecture","text":"<p>Overview of Markdown Reallocator's architecture and design.</p>"},{"location":"architecture/#system-architecture","title":"System Architecture","text":"<pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502                   CLI / API                  \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                   \u2502\n       \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n       \u2502                       \u2502\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u25bc\u2500\u2500\u2500\u2500\u2500\u2500\u2510         \u250c\u2500\u2500\u2500\u2500\u2500\u25bc\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502    Core     \u2502         \u2502  Modules   \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524         \u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502Preprocessor \u2502         \u2502  Search    \u2502\n\u2502  Splitter   \u2502         \u2502  Reorder   \u2502\n\u2502  Embedder   \u2502         \u2502   Dedup    \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2518         \u2514\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n       \u2502                      \u2502\n       \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                  \u2502\n          \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25bc\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n          \u2502     Models     \u2502\n          \u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n          \u2502  Chunk         \u2502\n          \u2502  Embedding     \u2502\n          \u2502  Exceptions    \u2502\n          \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                  \u2502\n          \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25bc\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n          \u2502     Utils      \u2502\n          \u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n          \u2502  Cache         \u2502\n          \u2502  Memory        \u2502\n          \u2502  Similarity    \u2502\n          \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"architecture/#core-components","title":"Core Components","text":"<ul> <li>Design Principles - Architectural decisions and rationale</li> <li>Data Flow - How data moves through the system</li> <li>Performance - Performance characteristics and optimization</li> </ul>"},{"location":"architecture/#key-design-decisions","title":"Key Design Decisions","text":""},{"location":"architecture/#modular-architecture","title":"Modular Architecture","text":"<p>Each component is independent and can be used standalone.</p>"},{"location":"architecture/#local-first","title":"Local-First","text":"<p>All processing happens locally without cloud dependencies.</p>"},{"location":"architecture/#memory-efficient","title":"Memory-Efficient","text":"<p>Automatic model loading/unloading to minimize GPU memory usage.</p>"},{"location":"architecture/#caching","title":"Caching","text":"<p>Embeddings are cached to avoid recomputation.</p>"},{"location":"architecture/data-flow/","title":"Data Flow","text":"<p>How data flows through Markdown Reallocator.</p>"},{"location":"architecture/data-flow/#pipeline-flow","title":"Pipeline Flow","text":"<pre><code>Input Markdown\n     \u2502\n     \u25bc\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502 Preprocessor\u2502  Fix structure\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n       \u2502\n       \u25bc\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502  Splitter   \u2502  Create chunks\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n       \u2502\n       \u25bc\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502  Embedder   \u2502  Generate vectors\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n       \u2502\n       \u25bc\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502   Modules   \u2502  Search/Reorder/Dedup\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n       \u2502\n       \u25bc\n  Output Markdown\n</code></pre>"},{"location":"architecture/data-flow/#data-models","title":"Data Models","text":""},{"location":"architecture/data-flow/#chunk","title":"Chunk","text":"<p>Contains document content and metadata: - <code>chunk_id</code>: Unique identifier - <code>content</code>: Markdown content - <code>metadata</code>: Hierarchy and position - <code>embedding</code>: Vector representation (optional)</p>"},{"location":"architecture/data-flow/#embedding","title":"Embedding","text":"<p>Vector representation with metadata: - <code>vector</code>: NumPy array - <code>text_hash</code>: Content hash - <code>model_name</code>: Embedding model</p> <p>See API Reference for complete details.</p>"},{"location":"architecture/design-principles/","title":"Design Principles","text":"<p>Core design principles guiding Markdown Reallocator.</p>"},{"location":"architecture/design-principles/#principles","title":"Principles","text":""},{"location":"architecture/design-principles/#1-local-first","title":"1. Local-First","text":"<p>All operations run locally without cloud dependencies.</p> <p>Benefits: - No API costs - Data privacy - Offline capability</p>"},{"location":"architecture/design-principles/#2-modular-design","title":"2. Modular Design","text":"<p>Each component is independent and reusable.</p> <p>Benefits: - Use only what you need - Easy testing - Clear separation of concerns</p>"},{"location":"architecture/design-principles/#3-resource-efficient","title":"3. Resource-Efficient","text":"<p>Designed for consumer hardware (2GB GPU).</p> <p>Strategies: - Automatic model unloading - Configurable batch sizes - Caching to reduce computation</p>"},{"location":"architecture/design-principles/#4-type-safe","title":"4. Type-Safe","text":"<p>Full type hints and mypy validation.</p> <p>Benefits: - Fewer runtime errors - Better IDE support - Self-documenting code</p>"},{"location":"architecture/design-principles/#5-well-tested","title":"5. Well-Tested","text":"<p>93%+ code coverage with unit, integration, and smoke tests.</p> <p>Benefits: - Reliable behavior - Safe refactoring - Regression prevention</p>"},{"location":"architecture/performance/","title":"Performance","text":"<p>Performance characteristics and optimization strategies.</p>"},{"location":"architecture/performance/#benchmarks","title":"Benchmarks","text":"<p>GTX 1050 (2GB) + Intel Xeon X5650:</p> Operation Time Throughput Preprocessing &lt;1s per 1000 lines CPU-bound Embedding (50 chunks) ~10s 300+ tokens/sec Search (1000 chunks) &lt;0.5s NumPy vectorized Reordering (100 chunks) &lt;2s Similarity + KMeans"},{"location":"architecture/performance/#optimization-strategies","title":"Optimization Strategies","text":""},{"location":"architecture/performance/#1-caching","title":"1. Caching","text":"<p>Embeddings are cached automatically: - Cache hits: instant retrieval - Cache location: <code>~/.cache/markdown-reallocator/</code></p>"},{"location":"architecture/performance/#2-batch-processing","title":"2. Batch Processing","text":"<p>Process multiple chunks at once: - Default batch size: 10 - Configurable per GPU</p>"},{"location":"architecture/performance/#3-model-management","title":"3. Model Management","text":"<p>Automatic loading/unloading: - Load on demand - Unload when memory threshold reached - Reduces GPU memory usage</p>"},{"location":"architecture/performance/#4-parallel-processing","title":"4. Parallel Processing","text":"<p>Use multiple CPU cores for preprocessing: <pre><code>from multiprocessing import Pool\n# Process files in parallel\n</code></pre></p>"},{"location":"architecture/performance/#bottlenecks","title":"Bottlenecks","text":"<ul> <li>Embedding: GPU-bound, ~10s for 50 chunks</li> <li>Network: Ollama connection overhead</li> </ul>"},{"location":"architecture/performance/#optimization-tips","title":"Optimization Tips","text":"<ol> <li>Enable caching</li> <li>Adjust batch size for your GPU</li> <li>Process files in batches</li> <li>Use faster embedding models for lower quality needs</li> </ol>"},{"location":"development/contributing/","title":"Contributing","text":"<p>Thank you for your interest in contributing to Markdown Reallocator!</p>"},{"location":"development/contributing/#getting-started","title":"Getting Started","text":"<ol> <li>Fork the repository</li> <li>Clone your fork</li> <li>Install development dependencies:</li> </ol> <pre><code>git clone https://github.com/yourusername/markdown-reallocator.git\ncd markdown-reallocator\npip install -e \".[dev]\"\n</code></pre>"},{"location":"development/contributing/#development-workflow","title":"Development Workflow","text":""},{"location":"development/contributing/#1-create-a-branch","title":"1. Create a Branch","text":"<pre><code>git checkout -b feature/your-feature-name\n</code></pre>"},{"location":"development/contributing/#2-make-changes","title":"2. Make Changes","text":"<ul> <li>Write code</li> <li>Add tests</li> <li>Update documentation</li> </ul>"},{"location":"development/contributing/#3-run-tests","title":"3. Run Tests","text":"<pre><code>pytest\n</code></pre>"},{"location":"development/contributing/#4-check-code-quality","title":"4. Check Code Quality","text":"<pre><code>ruff check .\nmypy markdown_reallocator\n</code></pre>"},{"location":"development/contributing/#5-submit-pull-request","title":"5. Submit Pull Request","text":"<ul> <li>Push to your fork</li> <li>Create pull request</li> <li>Describe your changes</li> </ul>"},{"location":"development/contributing/#guidelines","title":"Guidelines","text":""},{"location":"development/contributing/#code-style","title":"Code Style","text":"<ul> <li>Follow PEP 8</li> <li>Use type hints</li> <li>Write docstrings (Google style)</li> </ul>"},{"location":"development/contributing/#testing","title":"Testing","text":"<ul> <li>Add tests for new features</li> <li>Maintain 85%+ coverage</li> <li>Include smoke tests for major features</li> </ul>"},{"location":"development/contributing/#documentation","title":"Documentation","text":"<ul> <li>Update relevant docs</li> <li>Add examples for new features</li> <li>Keep README updated</li> </ul>"},{"location":"development/contributing/#questions","title":"Questions?","text":"<p>Open an issue or discussion on GitHub.</p>"},{"location":"development/deployment/","title":"Documentation Deployment","text":"<p>This project uses GitHub Pages to automatically deploy documentation from the main branch.</p>"},{"location":"development/deployment/#automatic-deployment","title":"Automatic Deployment","text":"<p>The documentation is automatically built and deployed on every push to the main branch via GitHub Actions.</p>"},{"location":"development/deployment/#workflow-configuration","title":"Workflow Configuration","text":"<p>The deployment workflow is defined in <code>.github/workflows/docs.yml</code>:</p> <pre><code>name: Deploy Documentation\n\non:\n  push:\n    branches:\n      - main\n  workflow_dispatch:\n</code></pre>"},{"location":"development/deployment/#steps","title":"Steps","text":"<ol> <li>Checkout: Fetches the repository code</li> <li>Setup Python: Installs Python 3.10</li> <li>Install Dependencies: Installs MkDocs and plugins via <code>pip install -e \".[docs]\"</code></li> <li>Build: Runs <code>mkdocs build --strict</code> to generate static site</li> <li>Deploy: Pushes the <code>site/</code> directory to the <code>gh-pages</code> branch</li> </ol>"},{"location":"development/deployment/#github-pages-configuration","title":"GitHub Pages Configuration","text":"<p>To enable GitHub Pages for this repository:</p> <ol> <li>Go to repository Settings \u2192 Pages</li> <li>Under \"Source\", select \"Deploy from a branch\"</li> <li>Select the <code>gh-pages</code> branch and <code>/ (root)</code> folder</li> <li>Click Save</li> </ol> <p>The documentation will be available at: <code>https://&lt;username&gt;.github.io/&lt;repository&gt;/</code></p>"},{"location":"development/deployment/#local-preview","title":"Local Preview","text":"<p>To preview the documentation locally:</p> <pre><code># Install documentation dependencies\npip install -e \".[docs]\"\n\n# Serve with live reload\nmkdocs serve\n\n# Build static site\nmkdocs build\n</code></pre> <p>The development server runs at <code>http://127.0.0.1:8000/</code></p>"},{"location":"development/deployment/#manual-deployment","title":"Manual Deployment","text":"<p>To manually deploy documentation:</p> <pre><code># Build and deploy to gh-pages branch\nmkdocs gh-deploy\n\n# Or trigger the GitHub Actions workflow\n# Go to Actions tab \u2192 Deploy Documentation \u2192 Run workflow\n</code></pre>"},{"location":"development/deployment/#troubleshooting","title":"Troubleshooting","text":""},{"location":"development/deployment/#build-fails-in-ci","title":"Build Fails in CI","text":"<p>If the workflow fails:</p> <ol> <li>Check the Actions tab for detailed error logs</li> <li>Verify all documentation files exist and are valid markdown</li> <li>Ensure <code>mkdocs.yml</code> navigation matches actual file structure</li> <li>Test locally with <code>mkdocs build --strict</code></li> </ol>"},{"location":"development/deployment/#404-on-github-pages","title":"404 on GitHub Pages","text":"<p>If the site shows a 404 error:</p> <ol> <li>Verify GitHub Pages is enabled in repository settings</li> <li>Check that the <code>gh-pages</code> branch exists and contains the site files</li> <li>Wait a few minutes for GitHub's CDN to update</li> <li>Check the repository visibility (public repos work best)</li> </ol>"},{"location":"development/deployment/#missing-styles-or-assets","title":"Missing Styles or Assets","text":"<p>If the site loads but looks broken:</p> <ol> <li>Check the <code>site_url</code> in <code>mkdocs.yml</code> matches your GitHub Pages URL</li> <li>Verify the <code>use_directory_urls</code> setting is appropriate</li> <li>Clear browser cache and reload</li> </ol>"},{"location":"development/release-process/","title":"Release Process","text":"<p>Steps for releasing new versions.</p>"},{"location":"development/release-process/#version-numbering","title":"Version Numbering","text":"<p>Follow semantic versioning (MAJOR.MINOR.PATCH): - MAJOR: Breaking changes - MINOR: New features (backward compatible) - PATCH: Bug fixes</p>"},{"location":"development/release-process/#release-steps","title":"Release Steps","text":"<ol> <li>Update version in <code>pyproject.toml</code></li> <li>Update CHANGELOG.md</li> <li>Run full test suite</li> <li>Build package</li> <li>Test installation</li> <li>Tag release</li> <li>Push to PyPI</li> </ol>"},{"location":"development/release-process/#example","title":"Example","text":"<pre><code># Update version\n# Edit pyproject.toml: version = \"0.2.0\"\n\n# Run tests\npytest\n\n# Build\npython -m build\n\n# Test\npip install dist/markdown_reallocator-0.2.0-py3-none-any.whl\n\n# Tag\ngit tag -a v0.2.0 -m \"Release v0.2.0\"\ngit push origin v0.2.0\n\n# Publish\npython -m twine upload dist/*\n</code></pre>"},{"location":"development/testing/","title":"Testing","text":"<p>Guide to running and writing tests.</p>"},{"location":"development/testing/#running-tests","title":"Running Tests","text":""},{"location":"development/testing/#all-tests","title":"All Tests","text":"<pre><code>pytest\n</code></pre>"},{"location":"development/testing/#specific-test-types","title":"Specific Test Types","text":"<pre><code># Smoke tests only (fast)\npytest tests/smoke/\n\n# Unit tests only\npytest tests/unit/\n\n# Integration tests\npytest tests/integration/\n</code></pre>"},{"location":"development/testing/#with-coverage","title":"With Coverage","text":"<pre><code>pytest --cov=markdown_reallocator --cov-report=html\n</code></pre>"},{"location":"development/testing/#writing-tests","title":"Writing Tests","text":""},{"location":"development/testing/#unit-tests","title":"Unit Tests","text":"<pre><code>def test_preprocessor():\n    preprocessor = MarkdownPreprocessor()\n    result = preprocessor.preprocess(\"**Title**\")\n    assert \"## Title\" in result\n</code></pre>"},{"location":"development/testing/#integration-tests","title":"Integration Tests","text":"<pre><code>@patch.object(Embedder, \"embed_chunk\")\ndef test_full_pipeline(mock_embed):\n    # Test complete workflow\n    pass\n</code></pre>"},{"location":"development/testing/#smoke-tests","title":"Smoke Tests","text":"<p>Quick validation tests that run in &lt;10 seconds:</p> <pre><code>def test_import_modules():\n    from markdown_reallocator.core import embedder\n    assert embedder is not None\n</code></pre>"},{"location":"development/testing/#test-coverage","title":"Test Coverage","text":"<p>Current coverage: 93.86%</p> <p>Goal: Maintain 85%+ coverage for all new code.</p>"},{"location":"examples/","title":"Examples","text":"<p>Real-world usage examples and patterns.</p>"},{"location":"examples/#available-examples","title":"Available Examples","text":"<ul> <li>Basic Workflow - Complete preprocessing to reordering</li> <li>Batch Processing - Process multiple files efficiently</li> <li>Custom Pipelines - Build custom processing workflows</li> <li>Integration - Integrate with existing tools</li> </ul>"},{"location":"examples/#quick-examples","title":"Quick Examples","text":""},{"location":"examples/#clean-llm-output","title":"Clean LLM Output","text":"<pre><code>markdown-reallocator preprocess llm_output.md --output clean.md\n</code></pre>"},{"location":"examples/#find-related-content","title":"Find Related Content","text":"<pre><code>markdown-reallocator split docs.md --output chunks.json\nmarkdown-reallocator embed chunks.json --output embeddings.npz\nmarkdown-reallocator search \"API authentication\" chunks.json embeddings.npz\n</code></pre>"},{"location":"examples/#remove-duplicates","title":"Remove Duplicates","text":"<pre><code>markdown-reallocator dedup chunks.json embeddings.npz --output clean.json --threshold 0.90\n</code></pre> <p>See individual example pages for detailed walkthroughs.</p>"},{"location":"examples/basic-workflow/","title":"Basic Workflow","text":"<p>Complete example of a typical markdown processing workflow.</p>"},{"location":"examples/basic-workflow/#workflow-steps","title":"Workflow Steps","text":"<ol> <li>Preprocess to fix structure</li> <li>Split into chunks</li> <li>Generate embeddings</li> <li>Search or reorder as needed</li> </ol> <p>See Quick Start for a detailed walkthrough.</p>"},{"location":"examples/batch-processing/","title":"Batch Processing","text":"<p>Process multiple markdown files efficiently.</p>"},{"location":"examples/batch-processing/#shell-script-example","title":"Shell Script Example","text":"<pre><code>#!/bin/bash\nfor file in inputs/*.md; do\n    markdown-reallocator preprocess \"$file\" --output \"outputs/$(basename \"$file\")\"\ndone\n</code></pre>"},{"location":"examples/batch-processing/#python-example","title":"Python Example","text":"<pre><code>from pathlib import Path\nfrom markdown_reallocator.core import MarkdownPreprocessor\n\npreprocessor = MarkdownPreprocessor()\n\nfor md_file in Path(\"inputs\").glob(\"*.md\"):\n    markdown = md_file.read_text()\n    clean = preprocessor.preprocess(markdown)\n    output_file = Path(\"outputs\") / md_file.name\n    output_file.write_text(clean)\n</code></pre>"},{"location":"examples/custom-pipelines/","title":"Custom Pipelines","text":"<p>Build custom processing workflows with the Python API.</p>"},{"location":"examples/custom-pipelines/#example-conditional-processing","title":"Example: Conditional Processing","text":"<pre><code>from markdown_reallocator.core import MarkdownPreprocessor, MarkdownSplitter, Embedder\nfrom markdown_reallocator.modules import DeduplicationModule\n\ndef process_document(markdown, remove_duplicates=True):\n    preprocessor = MarkdownPreprocessor()\n    splitter = MarkdownSplitter()\n    embedder = Embedder()\n\n    clean = preprocessor.preprocess(markdown)\n    chunks = splitter.split(clean)\n    embedded = embedder.embed_batch(chunks)\n\n    if remove_duplicates:\n        dedup = DeduplicationModule()\n        embedded, _ = dedup.deduplicate(embedded)\n\n    return embedded\n</code></pre> <p>See Python API Guide for more examples.</p>"},{"location":"examples/integration/","title":"Integration Examples","text":"<p>Integrate Markdown Reallocator with existing tools and frameworks.</p>"},{"location":"examples/integration/#fastapi-integration","title":"FastAPI Integration","text":"<pre><code>from fastapi import FastAPI\nfrom markdown_reallocator.core import MarkdownPreprocessor\n\napp = FastAPI()\npreprocessor = MarkdownPreprocessor()\n\n@app.post(\"/process\")\nasync def process(markdown: str):\n    return preprocessor.preprocess(markdown)\n</code></pre>"},{"location":"examples/integration/#langchain-integration","title":"LangChain Integration","text":"<p>See Python API Guide for LangChain integration examples.</p>"},{"location":"getting-started/configuration/","title":"Configuration","text":"<p>Markdown Reallocator provides flexible configuration through YAML files, environment variables, and Python API parameters.</p>"},{"location":"getting-started/configuration/#configuration-methods","title":"Configuration Methods","text":""},{"location":"getting-started/configuration/#1-default-configuration","title":"1. Default Configuration","text":"<p>Markdown Reallocator works out of the box with sensible defaults. No configuration required for most use cases!</p>"},{"location":"getting-started/configuration/#2-configuration-file","title":"2. Configuration File","text":"<p>Create <code>~/.config/markdown-reallocator/config.yaml</code>:</p> <pre><code># Preprocessor settings\npreprocessor:\n  max_title_length: 80\n  require_empty_lines: true\n  min_capitalization_ratio: 0.1\n\n# Splitter settings\nsplitter:\n  max_tokens_per_chunk: 1000\n  headers_to_split_on:\n    - [\"#\", \"h1\"]\n    - [\"##\", \"h2\"]\n    - [\"###\", \"h3\"]\n\n# Embedder settings\nembedder:\n  model_name: \"embeddinggemma\"\n  batch_size: 10\n  cache_enabled: true\n  memory_threshold_mb: 1500.0\n\n# Search settings\nsearch:\n  top_k: 5\n  min_similarity: 0.5\n  output_format: \"text\"\n\n# Reorder settings\nreorder:\n  strategy: \"sequential\"\n\n# Deduplication settings\ndedup:\n  similarity_threshold: 0.85\n  selection_strategy: \"first\"\n  use_llm: false\n  dry_run: false\n</code></pre>"},{"location":"getting-started/configuration/#3-project-specific-configuration","title":"3. Project-Specific Configuration","text":"<p>Place <code>config.yaml</code> in your project directory.</p> <p>Configuration search order (first found wins):</p> <ol> <li><code>./config.yaml</code> (current directory)</li> <li><code>./markdown_reallocator.yaml</code></li> <li><code>~/.config/markdown-reallocator/config.yaml</code></li> <li>Built-in defaults</li> </ol>"},{"location":"getting-started/configuration/#4-environment-variables","title":"4. Environment Variables","text":"<p>Override any setting with environment variables:</p> <pre><code># Pattern: MARKDOWN_REALLOCATOR_&lt;SECTION&gt;_&lt;KEY&gt;\n\n# Override embedding model\nexport MARKDOWN_REALLOCATOR_EMBEDDER_MODEL_NAME=\"nomic-embed-text\"\n\n# Override batch size\nexport MARKDOWN_REALLOCATOR_EMBEDDER_BATCH_SIZE=5\n\n# Override search parameters\nexport MARKDOWN_REALLOCATOR_SEARCH_TOP_K=10\nexport MARKDOWN_REALLOCATOR_SEARCH_MIN_SIMILARITY=0.7\n</code></pre> <p>Environment variables have highest priority and override file settings.</p>"},{"location":"getting-started/configuration/#configuration-sections","title":"Configuration Sections","text":""},{"location":"getting-started/configuration/#preprocessor","title":"Preprocessor","text":"<p>Controls markdown structure normalization.</p> <pre><code>preprocessor:\n  max_title_length: 80\n  require_empty_lines: true\n  min_capitalization_ratio: 0.1\n</code></pre> <p>Parameters:</p> <ul> <li><code>max_title_length</code> (int, default: 80): Maximum character length for bold text to be considered a heading</li> <li><code>require_empty_lines</code> (bool, default: true): Require empty lines around title candidates</li> <li><code>min_capitalization_ratio</code> (float, default: 0.1): Minimum proportion of capital letters in title</li> </ul>"},{"location":"getting-started/configuration/#splitter","title":"Splitter","text":"<p>Controls how documents are split into chunks.</p> <pre><code>splitter:\n  max_tokens_per_chunk: 1000\n  headers_to_split_on:\n    - [\"#\", \"h1\"]\n    - [\"##\", \"h2\"]\n    - [\"###\", \"h3\"]\n</code></pre> <p>Parameters:</p> <ul> <li><code>max_tokens_per_chunk</code> (int, default: 1000): Maximum tokens per chunk</li> <li><code>headers_to_split_on</code> (list, default: h1-h3): Markdown headers to split on</li> </ul>"},{"location":"getting-started/configuration/#embedder","title":"Embedder","text":"<p>Controls embedding generation.</p> <pre><code>embedder:\n  model_name: \"embeddinggemma\"\n  batch_size: 10\n  cache_enabled: true\n  memory_threshold_mb: 1500.0\n</code></pre> <p>Parameters:</p> <ul> <li><code>model_name</code> (str, default: \"embeddinggemma\"): Ollama embedding model</li> <li><code>batch_size</code> (int, default: 10): Chunks to embed per batch</li> <li><code>cache_enabled</code> (bool, default: true): Cache embeddings for identical content</li> <li><code>memory_threshold_mb</code> (float, default: 1500.0): Memory threshold for automatic model unloading</li> </ul> <p>Available Models:</p> <ul> <li><code>embeddinggemma</code> (621MB) - Recommended</li> <li><code>nomic-embed-text</code> (274MB) - Smaller</li> <li><code>all-minilm</code> (46MB) - Smallest</li> </ul>"},{"location":"getting-started/configuration/#search","title":"Search","text":"<p>Controls semantic search behavior.</p> <pre><code>search:\n  top_k: 5\n  min_similarity: 0.5\n  output_format: \"text\"\n</code></pre> <p>Parameters:</p> <ul> <li><code>top_k</code> (int, default: 5): Maximum number of results</li> <li><code>min_similarity</code> (float, default: 0.5): Minimum cosine similarity score</li> <li><code>output_format</code> (str, default: \"text\"): Output format for CLI</li> </ul>"},{"location":"getting-started/configuration/#reorder","title":"Reorder","text":"<p>Controls chunk reordering strategies.</p> <pre><code>reorder:\n  strategy: \"sequential\"\n</code></pre> <p>Parameters:</p> <ul> <li><code>strategy</code> (str, default: \"sequential\"): Reordering approach (\"sequential\" or \"cluster\")</li> </ul>"},{"location":"getting-started/configuration/#deduplication","title":"Deduplication","text":"<p>Controls duplicate detection and removal.</p> <pre><code>dedup:\n  similarity_threshold: 0.85\n  selection_strategy: \"first\"\n  use_llm: false\n  dry_run: false\n</code></pre> <p>Parameters:</p> <ul> <li><code>similarity_threshold</code> (float, default: 0.85): Cosine similarity threshold</li> <li><code>selection_strategy</code> (str, default: \"first\"): Which duplicate to keep (\"first\", \"longest\", or \"best_metadata\")</li> <li><code>use_llm</code> (bool, default: false): Use Gemma 2B to merge duplicates</li> <li><code>dry_run</code> (bool, default: false): Report without removing</li> </ul>"},{"location":"getting-started/configuration/#next-steps","title":"Next Steps","text":"<ul> <li>Quick Start - Try the configuration in practice</li> <li>User Guide - Learn about each module</li> </ul>"},{"location":"getting-started/installation/","title":"Installation","text":"<p>Guide for installing Markdown Reallocator and its dependencies.</p>"},{"location":"getting-started/installation/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.10+</li> <li>Ollama (download)</li> </ul>"},{"location":"getting-started/installation/#quick-install","title":"Quick Install","text":"<pre><code># Install Ollama models\nollama pull embeddinggemma\n\n# Install package\npip install markdown-reallocator\n</code></pre>"},{"location":"getting-started/installation/#verification","title":"Verification","text":"<pre><code>markdown-reallocator --version\n</code></pre> <p>See detailed installation steps and troubleshooting in the User Guide.</p>"},{"location":"getting-started/quickstart/","title":"Quick Start","text":"<p>Get started with Markdown Reallocator in 5 minutes.</p>"},{"location":"getting-started/quickstart/#basic-workflow","title":"Basic Workflow","text":""},{"location":"getting-started/quickstart/#1-preprocess","title":"1. Preprocess","text":"<p>Fix markdown structure:</p> <pre><code>markdown-reallocator preprocess input.md --output clean.md\n</code></pre>"},{"location":"getting-started/quickstart/#2-split","title":"2. Split","text":"<p>Create semantic chunks:</p> <pre><code>markdown-reallocator split clean.md --output chunks.json\n</code></pre>"},{"location":"getting-started/quickstart/#3-embed","title":"3. Embed","text":"<p>Generate embeddings:</p> <pre><code>markdown-reallocator embed chunks.json --output embeddings.npz\n</code></pre>"},{"location":"getting-started/quickstart/#4-search","title":"4. Search","text":"<p>Find related content:</p> <pre><code>markdown-reallocator search \"your query\" chunks.json embeddings.npz\n</code></pre>"},{"location":"getting-started/quickstart/#5-reorder","title":"5. Reorder","text":"<p>Organize by similarity:</p> <pre><code>markdown-reallocator reorder chunks.json embeddings.npz --output final.md\n</code></pre>"},{"location":"getting-started/quickstart/#python-api","title":"Python API","text":"<pre><code>from markdown_reallocator.core.preprocessor import MarkdownPreprocessor\nfrom markdown_reallocator.core.splitter import MarkdownSplitter\n\n# Load and preprocess\nmarkdown = open(\"input.md\").read()\npreprocessor = MarkdownPreprocessor()\nclean = preprocessor.preprocess(markdown)\n\n# Split\nsplitter = MarkdownSplitter()\nchunks = splitter.split(clean)\n</code></pre>"},{"location":"getting-started/quickstart/#next-steps","title":"Next Steps","text":"<ul> <li>User Guide - Learn all features</li> <li>Examples - See real-world usage</li> <li>API Reference - Complete API docs</li> </ul>"},{"location":"user-guide/","title":"User Guide","text":"<p>Complete guide to using Markdown Reallocator's features.</p>"},{"location":"user-guide/#overview","title":"Overview","text":"<p>Markdown Reallocator provides five core operations:</p> <ol> <li>Preprocessing - Fix markdown structure</li> <li>Splitting - Create semantic chunks</li> <li>Embeddings - Generate vector representations</li> <li>Search - Find similar content</li> <li>Reordering - Organize by similarity</li> <li>Deduplication - Remove redundant sections</li> </ol>"},{"location":"user-guide/#workflows","title":"Workflows","text":""},{"location":"user-guide/#document-cleanup","title":"Document Cleanup","text":"<p>Fix LLM-generated markdown with structural issues:</p> <pre><code>markdown-reallocator preprocess input.md --output clean.md\n</code></pre>"},{"location":"user-guide/#semantic-search","title":"Semantic Search","text":"<p>Find related content across documents:</p> <pre><code>markdown-reallocator split docs.md --output chunks.json\nmarkdown-reallocator embed chunks.json --output embeddings.npz\nmarkdown-reallocator search \"API authentication\" chunks.json embeddings.npz\n</code></pre>"},{"location":"user-guide/#content-organization","title":"Content Organization","text":"<p>Reorder scattered topics by similarity:</p> <pre><code>markdown-reallocator reorder chunks.json embeddings.npz \\\\\n  --strategy cluster --output organized.md\n</code></pre>"},{"location":"user-guide/#duplicate-removal","title":"Duplicate Removal","text":"<p>Clean up merged documents:</p> <pre><code>markdown-reallocator dedup chunks.json embeddings.npz \\\\\n  --threshold 0.90 --output clean.json\n</code></pre>"},{"location":"user-guide/#interfaces","title":"Interfaces","text":"<p>Markdown Reallocator provides two interfaces:</p>"},{"location":"user-guide/#cli","title":"CLI","text":"<p>Best for: - Quick one-off operations - Shell scripts and automation - Command-line workflows</p> <p>See CLI Reference for all commands.</p>"},{"location":"user-guide/#python-api","title":"Python API","text":"<p>Best for: - Custom pipelines - Integration with existing tools - Programmatic control</p> <p>See Python API Guide for examples.</p>"},{"location":"user-guide/#module-guides","title":"Module Guides","text":"<ul> <li> <p> Preprocessing</p> <p>Convert bold titles to headers, fix structure issues</p> </li> <li> <p> Splitting</p> <p>Break documents into semantic chunks with metadata</p> </li> <li> <p> Embeddings</p> <p>Generate vector representations for semantic operations</p> </li> <li> <p> Search</p> <p>Find similar chunks using natural language queries</p> </li> <li> <p> Reordering</p> <p>Reorganize chunks by semantic similarity</p> </li> <li> <p> Deduplication</p> <p>Remove redundant or near-duplicate sections</p> </li> </ul>"},{"location":"user-guide/#configuration","title":"Configuration","text":"<p>All modules support configuration via:</p> <ul> <li>YAML files (<code>~/.config/markdown-reallocator/config.yaml</code>)</li> <li>Environment variables (<code>MARKDOWN_REALLOCATOR_*</code>)</li> <li>Python API parameters</li> </ul> <p>See Configuration Guide for details.</p>"},{"location":"user-guide/#next-steps","title":"Next Steps","text":"<ul> <li>Start with Preprocessing to fix document structure</li> <li>Learn about Python API for custom workflows</li> <li>Check CLI Reference for all command options</li> </ul>"},{"location":"user-guide/cli-reference/","title":"CLI Reference","text":"<p>Complete command-line interface reference.</p>"},{"location":"user-guide/cli-reference/#global-options","title":"Global Options","text":"<pre><code>markdown-reallocator [OPTIONS] COMMAND [ARGS]\n</code></pre> <p>Options: - <code>--version</code>, <code>-v</code>: Show version and exit - <code>--verbose</code>, <code>-V</code>: Enable verbose output - <code>--no-color</code>: Disable colored output - <code>--help</code>: Show help message</p>"},{"location":"user-guide/cli-reference/#commands","title":"Commands","text":""},{"location":"user-guide/cli-reference/#preprocess","title":"preprocess","text":"<p>Fix markdown structure by converting bold titles to headers.</p> <pre><code>markdown-reallocator preprocess INPUT [OPTIONS]\n</code></pre> <p>Arguments: - <code>INPUT</code>: Input markdown file (required)</p> <p>Options: - <code>--output</code>, <code>-o PATH</code>: Output file (default: stdout) - <code>--detect-bold-headers/--no-detect-bold-headers</code>: Enable/disable bold header detection (default: enabled)</p> <p>Examples: <pre><code># Output to stdout\nmarkdown-reallocator preprocess input.md\n\n# Save to file\nmarkdown-reallocator preprocess input.md --output clean.md\n\n# Disable bold header detection\nmarkdown-reallocator preprocess input.md --no-detect-bold-headers\n</code></pre></p>"},{"location":"user-guide/cli-reference/#split","title":"split","text":"<p>Split markdown into semantic chunks.</p> <pre><code>markdown-reallocator split INPUT [OPTIONS]\n</code></pre> <p>Arguments: - <code>INPUT</code>: Input markdown file (required)</p> <p>Options: - <code>--output</code>, <code>-o PATH</code>: Output JSON file - <code>--max-tokens INT</code>: Maximum tokens per chunk (default: 1000)</p> <p>Examples: <pre><code># Output to stdout\nmarkdown-reallocator split input.md\n\n# Save to file with custom chunk size\nmarkdown-reallocator split input.md --output chunks.json --max-tokens 500\n</code></pre></p>"},{"location":"user-guide/cli-reference/#embed","title":"embed","text":"<p>Generate embeddings for chunks.</p> <pre><code>markdown-reallocator embed INPUT [OPTIONS]\n</code></pre> <p>Arguments: - <code>INPUT</code>: Input JSON file with chunks (required)</p> <p>Options: - <code>--output</code>, <code>-o PATH</code>: Output embeddings file (.npz) (required) - <code>--model TEXT</code>: Ollama model name (default: embeddinggemma) - <code>--cache/--no-cache</code>: Enable/disable caching (default: enabled)</p> <p>Examples: <pre><code># Generate embeddings\nmarkdown-reallocator embed chunks.json --output embeddings.npz\n\n# Use different model\nmarkdown-reallocator embed chunks.json --output embeddings.npz --model nomic-embed-text\n\n# Disable caching\nmarkdown-reallocator embed chunks.json --output embeddings.npz --no-cache\n</code></pre></p>"},{"location":"user-guide/cli-reference/#search","title":"search","text":"<p>Search for similar chunks.</p> <pre><code>markdown-reallocator search QUERY INPUT EMBEDDINGS [OPTIONS]\n</code></pre> <p>Arguments: - <code>QUERY</code>: Search query (required) - <code>INPUT</code>: Input JSON file with chunks (required) - <code>EMBEDDINGS</code>: Embeddings file (.npz) (required)</p> <p>Options: - <code>--top-k</code>, <code>-k INT</code>: Number of results (default: 5) - <code>--min-similarity FLOAT</code>: Minimum similarity (0.0-1.0) (default: 0.5) - <code>--format</code>, <code>-f TEXT</code>: Output format (text or json) (default: text)</p> <p>Examples: <pre><code># Basic search\nmarkdown-reallocator search \"authentication\" chunks.json embeddings.npz\n\n# More results, higher threshold\nmarkdown-reallocator search \"API security\" chunks.json embeddings.npz \\\\\n  --top-k 10 --min-similarity 0.7\n\n# JSON output\nmarkdown-reallocator search \"deployment\" chunks.json embeddings.npz --format json\n</code></pre></p>"},{"location":"user-guide/cli-reference/#reorder","title":"reorder","text":"<p>Reorder chunks by semantic similarity.</p> <pre><code>markdown-reallocator reorder INPUT EMBEDDINGS [OPTIONS]\n</code></pre> <p>Arguments: - <code>INPUT</code>: Input JSON file with chunks (required) - <code>EMBEDDINGS</code>: Embeddings file (.npz) (required)</p> <p>Options: - <code>--output</code>, <code>-o PATH</code>: Output markdown file (required) - <code>--strategy</code>, <code>-s TEXT</code>: Reordering strategy (sequential or cluster) (default: sequential)</p> <p>Examples: <pre><code># Sequential reordering\nmarkdown-reallocator reorder chunks.json embeddings.npz --output reordered.md\n\n# Cluster-based reordering\nmarkdown-reallocator reorder chunks.json embeddings.npz \\\\\n  --output reordered.md --strategy cluster\n</code></pre></p>"},{"location":"user-guide/cli-reference/#dedup","title":"dedup","text":"<p>Remove duplicate chunks.</p> <pre><code>markdown-reallocator dedup INPUT EMBEDDINGS [OPTIONS]\n</code></pre> <p>Arguments: - <code>INPUT</code>: Input JSON file with chunks (required) - <code>EMBEDDINGS</code>: Embeddings file (.npz) (required)</p> <p>Options: - <code>--output</code>, <code>-o PATH</code>: Output JSON file (required) - <code>--threshold</code>, <code>-t FLOAT</code>: Similarity threshold (0.0-1.0) (default: 0.85) - <code>--strategy</code>, <code>-s TEXT</code>: Selection strategy (first, longest, best_metadata) (default: first) - <code>--dry-run</code>: Show what would be removed without removing</p> <p>Examples: <pre><code># Basic deduplication\nmarkdown-reallocator dedup chunks.json embeddings.npz --output clean.json\n\n# Stricter threshold, keep longest\nmarkdown-reallocator dedup chunks.json embeddings.npz \\\\\n  --output clean.json --threshold 0.95 --strategy longest\n\n# Dry run to preview\nmarkdown-reallocator dedup chunks.json embeddings.npz --dry-run\n</code></pre></p>"},{"location":"user-guide/cli-reference/#configuration","title":"Configuration","text":"<p>CLI respects configuration files and environment variables:</p> <pre><code># Use custom config file\nMARKDOWN_REALLOCATOR_CONFIG=./custom_config.yaml markdown-reallocator preprocess input.md\n\n# Override settings via environment variables\nexport MARKDOWN_REALLOCATOR_EMBEDDER_BATCH_SIZE=5\nmarkdown-reallocator embed chunks.json --output embeddings.npz\n</code></pre>"},{"location":"user-guide/cli-reference/#exit-codes","title":"Exit Codes","text":"<ul> <li><code>0</code>: Success</li> <li><code>1</code>: Error (invalid arguments, file not found, processing error)</li> </ul>"},{"location":"user-guide/cli-reference/#next-steps","title":"Next Steps","text":"<ul> <li>Python API - Programmatic interface</li> <li>Configuration - Customize behavior</li> <li>Examples - Real-world usage patterns</li> </ul>"},{"location":"user-guide/deduplication/","title":"Deduplication","text":"<p>Remove duplicate or near-duplicate chunks intelligently.</p>"},{"location":"user-guide/deduplication/#overview","title":"Overview","text":"<p>Detect and remove redundant content based on embedding similarity: - Configurable similarity threshold - Multiple selection strategies - Optional LLM-based merging</p>"},{"location":"user-guide/deduplication/#usage","title":"Usage","text":""},{"location":"user-guide/deduplication/#cli","title":"CLI","text":"<pre><code>markdown-reallocator dedup chunks.json embeddings.npz \\\\\n  --output clean.json --threshold 0.85 --strategy first\n</code></pre> <p>Output: <pre><code>Deduplication Report\n====================\nOriginal chunks: 10\nDeduplicated chunks: 8\nRemoved: 2 duplicate(s)\n\nRemoved Chunks:\n- chunk_005: \"## Introduction...\" (95.2% similar to chunk_001)\n- chunk_008: \"## Overview...\" (87.1% similar to chunk_002)\n</code></pre></p>"},{"location":"user-guide/deduplication/#python-api","title":"Python API","text":"<pre><code>from markdown_reallocator.modules.dedup import DeduplicationModule\n\ndedup = DeduplicationModule(\n    similarity_threshold=0.85,\n    selection_strategy=\"first\",\n    dry_run=False\n)\n\ndeduplicated, report = dedup.deduplicate(embedded_chunks)\nprint(dedup.format_report(report))\n</code></pre>"},{"location":"user-guide/deduplication/#selection-strategies","title":"Selection Strategies","text":"<p>first (default) - Keep first occurrence - Simple and predictable</p> <p>longest - Keep chunk with most content - Preserves more information</p> <p>best_metadata - Keep chunk with complete hierarchy - Better for structured documents</p>"},{"location":"user-guide/deduplication/#llm-based-merging","title":"LLM-Based Merging","text":"<p>Use Gemma 2B to intelligently merge duplicates:</p> <pre><code>dedup:\n  use_llm: true\n  similarity_threshold: 0.85\n</code></pre> <pre><code>dedup = DeduplicationModule(use_llm=True)\ndeduplicated, report = dedup.deduplicate(chunks)\n</code></pre>"},{"location":"user-guide/deduplication/#dry-run-mode","title":"Dry Run Mode","text":"<p>Preview what would be removed:</p> <pre><code>markdown-reallocator dedup chunks.json embeddings.npz --dry-run\n</code></pre>"},{"location":"user-guide/deduplication/#configuration","title":"Configuration","text":"<pre><code>dedup:\n  similarity_threshold: 0.85\n  selection_strategy: \"first\"\n  use_llm: false\n  dry_run: false\n</code></pre>"},{"location":"user-guide/deduplication/#next-steps","title":"Next Steps","text":"<ul> <li>Reordering - Reorder after deduplication</li> <li>API Reference - Complete API documentation</li> </ul>"},{"location":"user-guide/embeddings/","title":"Embeddings","text":"<p>Generate vector representations for semantic operations using Ollama models.</p>"},{"location":"user-guide/embeddings/#overview","title":"Overview","text":"<p>Embeddings convert text chunks into high-dimensional vectors that capture semantic meaning, enabling: - Semantic search - Similarity-based reordering - Duplicate detection</p>"},{"location":"user-guide/embeddings/#usage","title":"Usage","text":""},{"location":"user-guide/embeddings/#cli","title":"CLI","text":"<pre><code>markdown-reallocator embed chunks.json --output embeddings.npz --cache\n</code></pre>"},{"location":"user-guide/embeddings/#python-api","title":"Python API","text":"<pre><code>from markdown_reallocator.core.embedder import Embedder\n\nembedder = Embedder(model_name=\"embeddinggemma\", cache_enabled=True)\nembedded_chunks = embedder.embed_batch(chunks)\n\n# Save embeddings\nembedder.save_embeddings(embedded_chunks, \"embeddings.npz\")\n</code></pre>"},{"location":"user-guide/embeddings/#models","title":"Models","text":"Model Size Quality Speed embeddinggemma 621MB Excellent Fast nomic-embed-text 274MB Good Very Fast all-minilm 46MB Basic Fastest"},{"location":"user-guide/embeddings/#caching","title":"Caching","text":"<p>Embeddings are cached automatically to avoid recomputation:</p> <pre><code>embedder = Embedder(cache_enabled=True)\nchunks1 = embedder.embed_batch(chunks)  # Generates embeddings\nchunks2 = embedder.embed_batch(chunks)  # Uses cache\n</code></pre> <p>Cache location: <code>~/.cache/markdown-reallocator/embeddings/</code></p>"},{"location":"user-guide/embeddings/#configuration","title":"Configuration","text":"<pre><code>embedder:\n  model_name: \"embeddinggemma\"\n  batch_size: 10\n  cache_enabled: true\n  memory_threshold_mb: 1500.0\n</code></pre>"},{"location":"user-guide/embeddings/#performance","title":"Performance","text":"<p>Typical speed on GTX 1050 (2GB): - 50 chunks: ~10 seconds - 300 tokens/second</p>"},{"location":"user-guide/embeddings/#next-steps","title":"Next Steps","text":"<ul> <li>Search - Use embeddings for semantic search</li> <li>Reordering - Reorganize by similarity</li> </ul>"},{"location":"user-guide/preprocessing/","title":"Preprocessing","text":"<p>The preprocessor fixes common structural issues in LLM-generated markdown, particularly converting bold-formatted titles to proper headings.</p>"},{"location":"user-guide/preprocessing/#problem","title":"Problem","text":"<p>LLMs frequently generate markdown with bold formatting instead of proper heading syntax:</p> <pre><code>**Introduction**\n\nThis is the introduction section.\n\n**Key Concepts**\n\nUnderstanding these concepts is important.\n</code></pre> <p>This breaks: - Document structure and navigation - Table of contents generation - Parsing by markdown tools - Screen readers and accessibility</p>"},{"location":"user-guide/preprocessing/#solution","title":"Solution","text":"<p>The preprocessor detects and converts standalone bold lines to proper headings:</p> <pre><code>## Introduction\n\nThis is the introduction section.\n\n## Key Concepts\n\nUnderstanding these concepts is important.\n</code></pre>"},{"location":"user-guide/preprocessing/#usage","title":"Usage","text":""},{"location":"user-guide/preprocessing/#cli","title":"CLI","text":"<pre><code>markdown-reallocator preprocess input.md --output clean.md\n</code></pre>"},{"location":"user-guide/preprocessing/#python-api","title":"Python API","text":"<pre><code>from markdown_reallocator.core.preprocessor import MarkdownPreprocessor\n\npreprocessor = MarkdownPreprocessor()\nmarkdown = open(\"input.md\").read()\nclean = preprocessor.preprocess(markdown)\n</code></pre>"},{"location":"user-guide/preprocessing/#detection-logic","title":"Detection Logic","text":"<p>The preprocessor uses multiple heuristics to distinguish title bold from emphasis bold:</p> <ol> <li>Empty lines: Title candidates have empty lines before and after</li> <li>Length: Titles are typically 3-80 characters</li> <li>Capitalization: Titles have higher capital letter ratio</li> <li>Position: First line or after empty line</li> <li>Content: No inline formatting or special characters</li> </ol>"},{"location":"user-guide/preprocessing/#example-detection","title":"Example Detection","text":"<pre><code># Detected as Title\n**Introduction**\n\n# Detected as Title  \n**Getting Started Guide**\n\n# Not a title (too long)\n**This is a very long sentence that contains more than eighty characters and is clearly not a heading**\n\n# Not a title (inline content)\nThis paragraph has **some bold text** for emphasis.\n\n# Not a title (no empty lines)\n**Bold**\nImmediately followed by content.\n</code></pre>"},{"location":"user-guide/preprocessing/#configuration","title":"Configuration","text":"<p>Customize detection behavior:</p> <pre><code># config.yaml\npreprocessor:\n  max_title_length: 80\n  require_empty_lines: true\n  min_capitalization_ratio: 0.1\n</code></pre>"},{"location":"user-guide/preprocessing/#parameters","title":"Parameters","text":"<p>max_title_length (int, default: 80) - Maximum character length for title candidates - Longer bold text is treated as emphasis</p> <p>require_empty_lines (bool, default: true) - Require empty lines around title candidates - Stricter but more accurate</p> <p>min_capitalization_ratio (float, default: 0.1) - Minimum proportion of capital letters - Helps identify proper nouns and titles</p>"},{"location":"user-guide/preprocessing/#examples","title":"Examples","text":""},{"location":"user-guide/preprocessing/#example-1-basic-cleanup","title":"Example 1: Basic Cleanup","text":"<p>Input: <pre><code>**Overview**\n\nThis document explains the API.\n\n**Authentication**\n\nUse OAuth 2.0 for authentication.\n</code></pre></p> <p>Output: <pre><code>## Overview\n\nThis document explains the API.\n\n## Authentication\n\nUse OAuth 2.0 for authentication.\n</code></pre></p>"},{"location":"user-guide/preprocessing/#example-2-preserving-emphasis","title":"Example 2: Preserving Emphasis","text":"<p>Input: <pre><code>## Introduction\n\nThis is **very important** information.\n\nYou should **always** read the documentation.\n</code></pre></p> <p>Output (unchanged): <pre><code>## Introduction\n\nThis is **very important** information.\n\nYou should **always** read the documentation.\n</code></pre></p>"},{"location":"user-guide/preprocessing/#example-3-mixed-content","title":"Example 3: Mixed Content","text":"<p>Input: <pre><code>**Section 1**\n\nSome text with **bold emphasis** in the middle.\n\n**Section 2**\n\nMore content here.\n</code></pre></p> <p>Output: <pre><code>## Section 1\n\nSome text with **bold emphasis** in the middle.\n\n## Section 2\n\nMore content here.\n</code></pre></p>"},{"location":"user-guide/preprocessing/#advanced-usage","title":"Advanced Usage","text":""},{"location":"user-guide/preprocessing/#custom-configuration","title":"Custom Configuration","text":"<pre><code>from markdown_reallocator.core.preprocessor import (\n    MarkdownPreprocessor,\n    PreprocessorConfig\n)\n\nconfig = PreprocessorConfig(\n    max_title_length=100,\n    require_empty_lines=False,\n    min_capitalization_ratio=0.2\n)\n\npreprocessor = MarkdownPreprocessor(config)\nresult = preprocessor.preprocess(markdown)\n</code></pre>"},{"location":"user-guide/preprocessing/#validation","title":"Validation","text":"<p>Check what will be converted:</p> <pre><code>preprocessor = MarkdownPreprocessor()\nmarkdown = \"**Title**\\n\\nContent\"\n\n# Get statistics\nstats = preprocessor.get_conversion_stats(markdown)\nprint(f\"Found {stats['titles_found']} titles\")\nprint(f\"Will convert {stats['titles_converted']} to headers\")\n</code></pre>"},{"location":"user-guide/preprocessing/#limitations","title":"Limitations","text":"<ul> <li>Cannot detect semantic intent (sometimes bold is genuinely for emphasis)</li> <li>Relies on structural patterns (empty lines, length, etc.)</li> <li>May misclassify edge cases</li> </ul> <p>For ambiguous cases, manual review is recommended.</p>"},{"location":"user-guide/preprocessing/#next-steps","title":"Next Steps","text":"<ul> <li>Splitting - Create semantic chunks from preprocessed markdown</li> <li>Configuration Guide - Customize preprocessor behavior</li> </ul>"},{"location":"user-guide/python-api/","title":"Python API","text":"<p>Build custom markdown processing pipelines with the Python API.</p>"},{"location":"user-guide/python-api/#overview","title":"Overview","text":"<p>The Python API provides: - Fine-grained control over each step - Easy integration with existing tools - Custom pipeline construction - Programmatic configuration</p>"},{"location":"user-guide/python-api/#basic-pipeline","title":"Basic Pipeline","text":"<pre><code>from pathlib import Path\nfrom markdown_reallocator.core.preprocessor import MarkdownPreprocessor\nfrom markdown_reallocator.core.splitter import MarkdownSplitter\nfrom markdown_reallocator.core.embedder import Embedder\n\n# Load markdown\nmarkdown = Path(\"input.md\").read_text()\n\n# Preprocess\npreprocessor = MarkdownPreprocessor()\nclean = preprocessor.preprocess(markdown)\n\n# Split\nsplitter = MarkdownSplitter(max_tokens_per_chunk=500)\nchunks = splitter.split(clean)\n\n# Embed\nembedder = Embedder(cache_enabled=True)\nembedded = embedder.embed_batch(chunks)\n\n# Save\nPath(\"output.md\").write_text(clean)\n</code></pre>"},{"location":"user-guide/python-api/#complete-pipeline","title":"Complete Pipeline","text":"<pre><code>from markdown_reallocator.core import MarkdownPreprocessor, MarkdownSplitter, Embedder\nfrom markdown_reallocator.modules import SearchModule, ReorderModule, DeduplicationModule\n\n# Setup\nmarkdown = Path(\"input.md\").read_text()\npreprocessor = MarkdownPreprocessor()\nsplitter = MarkdownSplitter()\nembedder = Embedder()\n\n# Process\nclean = preprocessor.preprocess(markdown)\nchunks = splitter.split(clean)\nembedded = embedder.embed_batch(chunks)\n\n# Deduplicate\ndedup = DeduplicationModule(similarity_threshold=0.85)\nunique_chunks, report = dedup.deduplicate(embedded)\n\n# Reorder\nreorder = ReorderModule(strategy=\"cluster\")\nreordered = reorder.reorder(unique_chunks)\nfinal = reorder.reconstruct_markdown(reordered)\n\n# Save\nPath(\"output.md\").write_text(final)\n</code></pre>"},{"location":"user-guide/python-api/#custom-configuration","title":"Custom Configuration","text":"<pre><code>from markdown_reallocator.config import Settings, PreprocessorConfig, EmbedderConfig\n\nsettings = Settings(\n    preprocessor=PreprocessorConfig(\n        max_title_length=100,\n        require_empty_lines=False\n    ),\n    embedder=EmbedderConfig(\n        model_name=\"nomic-embed-text\",\n        batch_size=5\n    )\n)\n\npreprocessor = MarkdownPreprocessor(config=settings.preprocessor)\nembedder = Embedder(model_name=settings.embedder.model_name)\n</code></pre>"},{"location":"user-guide/python-api/#batch-processing","title":"Batch Processing","text":"<p>Process multiple files:</p> <pre><code>from pathlib import Path\n\ninput_dir = Path(\"inputs/\")\noutput_dir = Path(\"outputs/\")\noutput_dir.mkdir(exist_ok=True)\n\nfor md_file in input_dir.glob(\"*.md\"):\n    markdown = md_file.read_text()\n    clean = preprocessor.preprocess(markdown)\n\n    output_file = output_dir / md_file.name\n    output_file.write_text(clean)\n</code></pre>"},{"location":"user-guide/python-api/#error-handling","title":"Error Handling","text":"<pre><code>try:\n    embedded = embedder.embed_batch(chunks)\nexcept ConnectionError as e:\n    print(f\"Ollama not running: {e}\")\nexcept MemoryError as e:\n    print(f\"Out of memory: {e}\")\n    # Reduce batch size\n    embedder.config.batch_size = 5\n    embedded = embedder.embed_batch(chunks)\n</code></pre>"},{"location":"user-guide/python-api/#integration-examples","title":"Integration Examples","text":""},{"location":"user-guide/python-api/#with-langchain","title":"With LangChain","text":"<pre><code>from langchain.text_splitter import MarkdownHeaderTextSplitter\nfrom markdown_reallocator.core import Embedder\n\n# Use LangChain for splitting\nsplitter = MarkdownHeaderTextSplitter(headers_to_split_on=[(\"#\", \"h1\"), (\"##\", \"h2\")])\nlangchain_chunks = splitter.split_text(markdown)\n\n# Convert to Markdown Reallocator format\nfrom markdown_reallocator.models.chunk import Chunk, ChunkMetadata\nchunks = [\n    Chunk(chunk_id=f\"chunk_{i}\", content=c.page_content, \n          metadata=ChunkMetadata(original_position=i))\n    for i, c in enumerate(langchain_chunks)\n]\n\n# Embed with Markdown Reallocator\nembedder = Embedder()\nembedded = embedder.embed_batch(chunks)\n</code></pre>"},{"location":"user-guide/python-api/#with-fastapi","title":"With FastAPI","text":"<pre><code>from fastapi import FastAPI, UploadFile\nfrom markdown_reallocator.core import MarkdownPreprocessor\n\napp = FastAPI()\npreprocessor = MarkdownPreprocessor()\n\n@app.post(\"/preprocess\")\nasync def preprocess_markdown(file: UploadFile):\n    markdown = await file.read()\n    clean = preprocessor.preprocess(markdown.decode())\n    return {\"processed\": clean}\n</code></pre>"},{"location":"user-guide/python-api/#next-steps","title":"Next Steps","text":"<ul> <li>CLI Reference - Command-line interface</li> <li>API Reference - Complete API documentation</li> <li>Examples - Real-world use cases</li> </ul>"},{"location":"user-guide/reordering/","title":"Reordering","text":"<p>Reorganize document chunks by semantic similarity for better topical flow.</p>"},{"location":"user-guide/reordering/#overview","title":"Overview","text":"<p>Two reordering strategies: - Sequential: Chain similar chunks one after another - Cluster: Group chunks by topic, then order within groups</p>"},{"location":"user-guide/reordering/#usage","title":"Usage","text":""},{"location":"user-guide/reordering/#cli","title":"CLI","text":"<pre><code># Sequential reordering\nmarkdown-reallocator reorder chunks.json embeddings.npz \\\\\n  --strategy sequential --output reordered.md\n\n# Cluster-based reordering\nmarkdown-reallocator reorder chunks.json embeddings.npz \\\\\n  --strategy cluster --output reordered.md\n</code></pre>"},{"location":"user-guide/reordering/#python-api","title":"Python API","text":"<pre><code>from markdown_reallocator.modules.reorder import ReorderModule\n\n# Sequential\nreorder = ReorderModule(strategy=\"sequential\")\nreordered = reorder.reorder(embedded_chunks)\nmarkdown = reorder.reconstruct_markdown(reordered)\n\n# Cluster\nreorder = ReorderModule(strategy=\"cluster\")\nreordered = reorder.reorder(embedded_chunks)\nmarkdown = reorder.reconstruct_markdown(reordered, include_comments=True)\n</code></pre>"},{"location":"user-guide/reordering/#strategies","title":"Strategies","text":""},{"location":"user-guide/reordering/#sequential","title":"Sequential","text":"<p>Chains chunks by similarity: 1. Start with first chunk (or seed) 2. Find most similar unchosen chunk 3. Repeat until all chunks processed</p> <p>Best for: Gradual topic progression</p>"},{"location":"user-guide/reordering/#cluster","title":"Cluster","text":"<p>Groups similar chunks: 1. Cluster chunks by embedding similarity (KMeans) 2. Order clusters by size or centrality 3. Order chunks within each cluster</p> <p>Best for: Distinct topic groups</p>"},{"location":"user-guide/reordering/#configuration","title":"Configuration","text":"<pre><code>reorder:\n  strategy: \"sequential\"  # or \"cluster\"\n</code></pre>"},{"location":"user-guide/reordering/#next-steps","title":"Next Steps","text":"<ul> <li>Deduplication - Remove duplicates before reordering</li> <li>API Reference - Complete API documentation</li> </ul>"},{"location":"user-guide/search/","title":"Semantic Search","text":"<p>Find chunks similar to queries or other chunks using embedding-based similarity.</p>"},{"location":"user-guide/search/#overview","title":"Overview","text":"<p>Semantic search finds related content based on meaning, not just keywords: - Natural language queries - Ranked by cosine similarity - Adjustable thresholds and result counts</p>"},{"location":"user-guide/search/#usage","title":"Usage","text":""},{"location":"user-guide/search/#cli","title":"CLI","text":"<pre><code>markdown-reallocator search \"authentication methods\" \\\\\n  chunks.json embeddings.npz --top-k 5 --min-similarity 0.5\n</code></pre> <p>Output: <pre><code>Found 3 results:\n\n1. Score: 0.892\n   ## API Authentication\n   Use OAuth 2.0 for secure authentication...\n\n2. Score: 0.745\n   ## Security Best Practices\n   Always validate tokens and use HTTPS...\n</code></pre></p>"},{"location":"user-guide/search/#python-api","title":"Python API","text":"<pre><code>from markdown_reallocator.modules.search import SearchModule\nfrom markdown_reallocator.core.embedder import Embedder\n\nembedder = Embedder()\nsearch = SearchModule(embedder=embedder, top_k=5, min_similarity=0.5)\n\nresults = search.search(\"authentication methods\", embedded_chunks)\n\nfor chunk, score in results:\n    print(f\"Score: {score:.3f}\")\n    print(chunk.content[:100])\n</code></pre>"},{"location":"user-guide/search/#configuration","title":"Configuration","text":"<pre><code>search:\n  top_k: 5\n  min_similarity: 0.5\n  output_format: \"text\"  # or \"json\"\n</code></pre>"},{"location":"user-guide/search/#advanced-usage","title":"Advanced Usage","text":""},{"location":"user-guide/search/#find-similar-chunks","title":"Find Similar Chunks","text":"<p>Find chunks similar to a given chunk:</p> <pre><code>target_chunk = embedded_chunks[0]\nsimilar = search.find_similar_chunks(target_chunk, embedded_chunks, top_k=5)\n</code></pre>"},{"location":"user-guide/search/#custom-similarity-function","title":"Custom Similarity Function","text":"<pre><code>from markdown_reallocator.utils.similarity import cosine_similarity\n\n# Manual similarity calculation\nsimilarity = cosine_similarity(chunk1.embedding, chunk2.embedding)\n</code></pre>"},{"location":"user-guide/search/#use-cases","title":"Use Cases","text":"<ul> <li>Documentation search</li> <li>Related content discovery</li> <li>Duplicate detection preprocessing</li> <li>Content recommendation</li> </ul>"},{"location":"user-guide/search/#next-steps","title":"Next Steps","text":"<ul> <li>Reordering - Organize search results by similarity</li> <li>API Reference - Complete API documentation</li> </ul>"},{"location":"user-guide/splitting/","title":"Splitting","text":"<p>Split markdown documents into semantic chunks while preserving heading hierarchy.</p>"},{"location":"user-guide/splitting/#overview","title":"Overview","text":"<p>The splitter breaks documents at markdown headers, creating chunks that: - Maintain semantic coherence - Include metadata (h1, h2, h3, position) - Respect token limits - Preserve hierarchy context</p>"},{"location":"user-guide/splitting/#usage","title":"Usage","text":""},{"location":"user-guide/splitting/#cli","title":"CLI","text":"<pre><code>markdown-reallocator split input.md --output chunks.json --max-tokens 1000\n</code></pre>"},{"location":"user-guide/splitting/#python-api","title":"Python API","text":"<pre><code>from markdown_reallocator.core.splitter import MarkdownSplitter\n\nsplitter = MarkdownSplitter(max_tokens_per_chunk=1000)\nchunks = splitter.split(markdown_content)\n\nfor chunk in chunks:\n    print(f\"ID: {chunk.chunk_id}\")\n    print(f\"Position: {chunk.metadata.original_position}\")\n    print(f\"Hierarchy: {chunk.metadata.h1} &gt; {chunk.metadata.h2}\")\n</code></pre>"},{"location":"user-guide/splitting/#how-it-works","title":"How It Works","text":"<ol> <li>Parse markdown headers (#, ##, ###)</li> <li>Split at headers while respecting token limits</li> <li>Attach metadata to each chunk</li> <li>Generate unique IDs</li> </ol>"},{"location":"user-guide/splitting/#configuration","title":"Configuration","text":"<pre><code>splitter:\n  max_tokens_per_chunk: 1000\n  headers_to_split_on:\n    - [\"#\", \"h1\"]\n    - [\"##\", \"h2\"]\n    - [\"###\", \"h3\"]\n</code></pre>"},{"location":"user-guide/splitting/#output-format","title":"Output Format","text":"<p>Each chunk contains: - <code>chunk_id</code>: Unique identifier - <code>content</code>: Markdown content - <code>metadata</code>: Heading hierarchy and position - <code>embedding</code>: Null (populated by embedder)</p>"},{"location":"user-guide/splitting/#next-steps","title":"Next Steps","text":"<ul> <li>Embeddings - Generate vector representations for chunks</li> <li>API Reference - Complete API documentation</li> </ul>"}]}